# Dashboard CHANGELOG

Registro automático de melhorias aplicadas pelo `e2_web_platform_agent_job`.

Cada entrada representa uma run diária (07:00) com uma melhoria implementada, validada e auto-avaliada pelo agente Claude. O agente opera em Tiers de qualidade crescente:

- **Tier 1** — Fundação visual e usabilidade
- **Tier 2** — Gráficos com maior densidade informativa
- **Tier 3** — Home page executiva
- **Tier 4** — Comercialização (bloqueado até 2º cliente)
- **Tier 5** — Preditivo aprofundado

## 2026-05-29 07:00
- **[Tier 1]** Adicionar @st.cache_data com TTL=300 para queries de parquet
- Files: `dashboard/pages/1_SAV.py`
- Rationale: Fallback seguro: caching reduz latência em carregamentos repetidos
- Self-eval: alignment=5, value=3, quality=7, risk=4

## 2026-05-29 07:00
- **[Tier 1]** Adicionar @st.cache_data com TTL=300 para queries de parquet
- Files: `dashboard/pages/1_SAV.py`
- Rationale: Fallback seguro: caching reduz latência em carregamentos repetidos
- Self-eval: alignment=5, value=3, quality=7, risk=4

## 2026-05-30 07:00
- **[Tier 1]** Mover 'from datetime import date' (atualmente na linha 232, no fim do arquivo) para o topo, junto aos demais imports. Hoje 'date.today()' é chamado na linha 226 (st.download_button do Event Log) ANTES de o import ser executado.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: Bug latente real, não otimização prematura: o Streamlit executa o script de cima para baixo, então quando 'any_exceedance' tem ao menos um registro, df_exc não fica vazio e a linha 226 chama date.today() — mas 'date' só é importado na linha 232. Resultado: NameError e crash da página EXATAMENTE quando há exceedances ATA 05 reais (a situação mais crítica de segurança, em que o gerente de manutenção mais precisa do log e do export CSV). É a mudança de maior valor e menor risco disponível, alinhada às lições aprendidas (priorizar valor visível ao operador sobre infra invisível) e não toca em 1_SAV.py (alvo das 2 runs anteriores, conforme regra 3).
- Self-eval: alignment=7, value=7, quality=7, risk=3

## 2026-05-30 07:00
- **[Tier 1]** Add a 4th KPI card 'Aircraft >5% above fleet avg cruise burn' to the existing KPI row, reusing the same 1.05× fleet-mean threshold already used in Section 2, with a contextual help tooltip explaining the degradation signal.
- Files: `dashboard/pages/4_Combustivel.py`
- Rationale: Every page already has KPI cards (the gaps report is stale), so the real Tier 1 gap is that this page's KPIs are descriptive (flights, avg kg) not actionable. The page already computes which aircraft burn >5% above fleet average for a chart; surfacing that count as a header KPI gives a maintenance manager an instant fleet-health signal. Diversified away from SAV/FOQA (last 3 runs), low risk, ~15 lines, all-English, extends rather than replaces.
- Self-eval: alignment=6, value=5, quality=7, risk=2

## 2026-05-30 07:00
- **[Tier 1]** Replace the fleet-relative KPI card (c4 'Aircraft >5% above fleet avg cruise burn') with a per-tail degradation signal: count of aircraft whose own cruise-burn linear trend rises >5% across the analyzed window (np.polyfit slope vs. the aircraft's own baseline mean). Centralize the repeated df[cruise_cols].sum(axis=1) into a single df['_cruise_total'] column computed once after cruise_cols is defined, and reuse it in the KPIs and Sections 2/3/4.
- Files: `dashboard/pages/4_Combustivel.py`
- Rationale: Directly applies the validated lesson from the 2026-05-30 run: a fleet-relative threshold is statistically guaranteed to flag aircraft (mere dispersion, not degradation) and merely echoes Section 2. A per-tail rising-trend signal uses each aircraft's own baseline — an actionable maintenance trigger, not noise. Centralizing _cruise_total (recomputed at lines 75, 83, 154, 195, 228) removes divergence risk between KPI and charts. Both fixes are explicitly documented in lessons_learned. All other pages already have KPI cards, so the gaps metadata claiming otherwise is stale; the real Tier-1 quality gap is this misleading KPI.
- Self-eval: alignment=7, value=6, quality=6, risk=2

## 2026-05-30 07:00
- **[Tier 1]** Add a data-freshness header caption under the SAV title showing the latest flight date present in the loaded dataset (max 'date' across LH+RH, computed before filtering, guarded against empty/NaT), e.g. 'Data through 28-May-2026 · auto-refreshed hourly'.
- Files: `dashboard/pages/1_SAV.py`
- Rationale: 1_SAV.py is the only page carrying @st.cache_data(ttl=300), the exact silent-staleness risk flagged repeatedly in runs 2026-05-29 (cache added without any 'last updated' indicator). The top recurring lesson is that in safety-critical aeronautical dashboards data freshness must be explicit and visible. Exposing the dataset's latest flight date directly remediates that documented risk, is the genuine source of freshness available (data comes from Google Drive via load(), so no local file mtime exists), and advances the open Tier 1 'standardized header' item. Diversifies away from Combustivel/FOQA (last 3 runs) and is high-confidence/low-risk.
- Self-eval: alignment=7, value=6, quality=6, risk=2

## 2026-05-31 07:00
- **[Tier 1]** Replace the hardcoded cadence claim 'Updated at 07:00 and 19:00' in the subtitle caption (line 15) with a derived staleness indicator computed from the briefing file's real mtime: show the briefing age and color-warn the operator when it is stale. Keep the existing 'Last updated: <timestamp>' line but add an age delta (e.g. 'Generated 3h ago' / 'Generated 2 days ago — pipeline may be stale') and render it via st.success when fresh (<= 18h, covering the ~12h schedule gap) or st.warning when stale (> 18h). All text in English.
- Files: `dashboard/pages/6_Fleet_Briefing.py`
- Rationale: Documented transversal lesson: freshness claims must be derived from real refresh state, never hardcoded cadence strings that keep asserting 'hourly/07:00 & 19:00' even after the e2_web_platform_agent_job crashes. This is the weakest dashboard page (44 lines, no sidebar/KPIs) and is not blocked by rule 3 (last 3 runs touched 4_Combustivel x2 and 1_SAV). The fix turns a misleading static label into an actionable safety-relevant signal: an operator instantly sees whether the AI briefing reflects current fleet state or is days old. mtime is already loaded (line 21), so the change is minimal and self-contained.
- Self-eval: alignment=7, value=7, quality=6, risk=2

## 2026-06-01 07:00
- **[Tier 1]** Add a data-freshness indicator under the page title showing the latest flight date in the loaded dataset (max of 'date', computed before the days_back filter, guarded against empty/NaT) plus the age delta in days vs. today, rendered via st.success when recent (<= 2 days) or st.warning when stale (> 2 days). No hardcoded cadence claim — the indicator is derived purely from the real max event date.
- Files: `dashboard/pages/2_Rodas_e_Freios.py`
- Rationale: Diversifies target (last 3 runs touched 1_SAV/4_Combustivel/5_FOQA/6_Fleet, not this page) and applies the documented transversal freshness lesson: a freshness indicator was added only to 1_SAV and 6_Fleet, leaving the other 4 pages inconsistent. This is a safety-critical operational page where the maintenance manager must know if wheel/brake data is stale. Following the lessons, it distinguishes 'latest event date' from refresh, avoids any hardcoded cadence string, and skips premature caching (no perf complaint).
- Self-eval: alignment=7, value=6, quality=7, risk=3

## 2026-06-02 07:00
- **[Tier 1]** Replace the hardcoded cadence claim in the subtitle caption (line 28, 'Refreshed with every TCRF decode') with a derived data-freshness indicator: compute the latest flight date (max of the already-coerced df['date'], guarded against empty/NaT) and the age delta in days vs. today, rendered via st.success when fresh (<=3 days, justified by the daily per-MSN flight cadence) or st.warning when stale ('No new flights ingested in N days — pipeline may be stale'). Keep a static descriptive caption only for the monitored-parameter list, with no cadence assertion. Also fix the leftover Portuguese label '⬇️ Exportar CSV' (line 228) to English '⬇️ Export CSV' per the English-only rule.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: FOQA hasn't been touched in the last 5 runs (rule 3 satisfied) and its caption carries exactly the hardcoded-cadence claim the freshness lessons warn against — it would keep asserting freshness even if e2_foqa_moqa_job fails. Deriving the indicator from the real max event date applies the documented transversal lesson without a magic threshold (3 days is anchored to the known daily flight cadence), distinguishes 'latest event date' from a false cadence assertion, and reuses the existing pd.to_datetime coercion (line 37) so the dtype-safety lesson is honored. Fixing 'Exportar CSV' closes an English-only rule violation in the same file with negligible risk.
- Self-eval: alignment=7, value=6, quality=7, risk=3

## 2026-06-03 07:00
- **[Tier 2]** Add a 'Fleet Exceedance Heatmap' section right below the fleet KPIs (before the existing tabs): a Plotly heatmap with aircraft (MSN) on the Y axis and exceedance parameters (ITT T/O, N2 vib amber, hard landing, VMO, VLE gear, APU EGT) on the X axis, where each cell is the count of flights in the filtered period that tripped that flag. Build the matrix defensively from the existing boolean flag columns (guard each with df.get(...) and skip columns entirely absent), combine LH/RH ITT and N2 into a single parameter each, render with a white→amber→red colorscale and per-cell hovertemplate showing 'MSN — parameter: N flights'. If no exceedance flags or no positive counts exist, show a polite st.success empty state. All text in English.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: Tier 1 is saturated (90%, no advance-blocking) so Tier 2 is unblocked at 0% — this delivers its flagship, demo-worthy item ('Heatmap de excedências por aeronave × parâmetro'). It directly answers the most-repeated lesson across the last 5 runs: aggregate counts are not actionable unless they expose the specific MSNs. The heatmap turns the existing scalar KPI cards into a per-tail, per-parameter hotspot map a maintenance manager can act on instantly. It reuses flag columns already present (itt_*_takeoff_exceedance, n2_vib_*_amber, hard_landing_flag, vmo_exceedance, vle_exceedance, apu_egt_exceedance), so no invented data, and FOQA is the only page with multi-parameter exceedance data so it is the correct home. Last 5 runs were all status/freshness-badge tweaks on 5 different pages; this breaks that pattern with real operational insight.
- Self-eval: alignment=8, value=8, quality=7, risk=2

## 2026-06-04 07:00
- **[Tier 2]** Append a new 'Section 5: Per-Aircraft Cruise-Burn Degradation vs Own Baseline' with a drill-down MSN selectbox. For the chosen tail, plot its per-flight cruise fuel over time and overlay an absolute reference baseline (median of that tail's earliest ~30% of flights, min 5 flights required), plus a shaded amber tolerance band (baseline ×1.05–×1.10) and a red zone (> ×1.10). Color each flight marker green/amber/red against its own baseline and show an actionable st.warning/st.success verdict ('MSN X is burning N% above its own baseline — inspect for engine degradation' vs 'within tolerance'). Guard against <5 flights and empty data with a polite empty state. All text in English.
- Files: `dashboard/pages/4_Combustivel.py`
- Rationale: Diversification rule blocks 5_FOQA (last 2 runs); 4_Combustivel hasn't been touched and Tier 1 is saturated (90%), so Tier 2 is in scope. This single change lands three Tier 2 backlog items at once — degradation timeline per component, amber/red threshold zones, and per-aircraft drill-down — and directly applies documented lessons: it anchors the signal to each tail's OWN absolute baseline (not the fleet-relative ×1.05 coloring in Section 2 that is statistically guaranteed to trip), requires ≥5 flights before emitting a verdict for robustness, and turns observation into action ('what should I do'). It is pure extension appended after Section 4, isolated and degrading gracefully.
- Self-eval: alignment=6, value=6, quality=6, risk=2

## 2026-06-05 07:00
- **[Tier 2]** Add contextual hover tooltips to the 5 SAV degradation trend charts in `_trend_chart`. Pass `custom_data=['ac_sn']` to px.scatter and set a hovertemplate on the marker traces (selector mode='markers') showing: MSN (bold), flight date (%d-%b-%Y), the signal value formatted with its unit, the degradation-direction context (rising/falling is bad), and the computed P90/P10 degradation threshold for that signal. The trendline trace keeps its default hover. No data invented; all strings in English.
- Files: `dashboard/pages/1_SAV.py`
- Rationale: Tier 2 is at 20% and must be advanced before Tier 3 (rule 1). The next unaddressed Tier 2 backlog item is 'Tooltips com contexto (limite, MSN, data)'. 1_SAV.py has 9 charts but `has_tooltips=false` and has not been touched in the last 5 runs, satisfying rule 3 (avoid 5_FOQA_MOQA / 4_Combustivel, recently edited). The change embeds the per-point context the operator needs to act (which tail, when, how far into the degradation zone), aligning with product principle #2 'contexto sempre visível' and the lesson that actionable insight beats cosmetic tweaks. It is isolated to one helper function, degrades gracefully, and reuses the already-computed `p_bad` threshold so the tooltip and the visual hrect cannot diverge.
- Self-eval: alignment=7, value=6, quality=7, risk=2

## 2026-06-05 07:00
- **[Tier 2]** Rebuild Section 2's 'Alert Rate Heatmap — MSN × Wheel Position' using px.imshow on the existing wide matrix (ac_sn index × position columns) with a fixed color scale (zmin=0, zmax=100) and per-cell annotated values (text_auto='.0f'), replacing the current px.density_heatmap whose Plotly auto-normalization makes a 5% alert-rate cell look as red as a 90% one. Keep the same green→amber→red colorscale, MSN/position labels, dynamic height, and a polite empty state if no positive alert rates exist.
- Files: `dashboard/pages/2_Rodas_e_Freios.py`
- Rationale: Diversifies away from the last 3 runs (5_FOQA, 4_Combustivel, 1_SAV) onto a page untouched recently, and directly closes a documented lesson ('Em heatmaps de contagem, fixar zmin/zmax ou anotar valores nas células evita que a normalização automática do Plotly engane a percepção de severidade'). This exact misleading pattern lives in Section 2 of this page: severity is currently relative to the matrix max, so a single-alert tail can read as critically red as a chronically failing one — a real misread risk in a paid safety product. Fixing the scale to absolute 0–100% plus on-cell counts makes severity comparable across aircraft and exact, which is a correctness/triage improvement (which tail to act on first), not gratuitous polish.
- Self-eval: alignment=8, value=7, quality=8, risk=2

## 2026-06-06 07:00
- **[Tier 2]** Enhance Section 4 (Dispatch Forecast) into an actionable maintenance-planning triage. Add a sidebar 'Planning horizon (days)' slider (default 30). Reusing the single existing df_fc computation (no recompute/divergence), add an 'Estimated date' column (today + days-to-amber) and render a prioritized triage banner ABOVE the detail table: st.error listing the specific MSNs currently below 845 PSI ('Immediate — no dispatch, QRH action required'), st.warning listing MSNs forecast to cross the 845 PSI amber threshold within the planning horizon with their estimated calendar dates, and st.success when none require action. Only flag aircraft where avg daily drop > 0 (finite forecast); guard empty df_fc. All text in English.
- Files: `dashboard/pages/3_Oxigenio.py`
- Rationale: 3_Oxigenio is the only eligible page untouched in the last 5 runs (diversification per rules) and the most mature, so a focused actionable feature lands cleanly. The lessons repeatedly say to prioritize 'what should I do' over status/cosmetics, and that count KPIs have limited value unless they expose the specific entities — this converts the existing forecast table into a triage that names the exact MSNs to act on and when, reusing the same df_fc to avoid KPI/table divergence. Additive and isolated, so no regression risk to existing charts.
- Self-eval: alignment=7, value=7, quality=6, risk=4

## 2026-06-07 07:00
- **[Tier 2]** Add a 'Per-Aircraft Exceedance Drill-Down' section immediately after the Fleet Exceedance Heatmap (before the tabs), giving the natural 'see hotspot → drill into that tail' flow. Add an MSN selectbox (options = sorted unique ac_sn from the filtered df; default to the tail with the most exceedances). For the selected MSN, build a chronological event table ONLY from exceedance flag columns already present in df, where each row = date, Parameter (human label), Recorded value (the matching measured column, e.g. max_itt_lh_takeoff for ITT T/O, max_n2_vib_lh/rh for N2 vib, max_normal_accel_landing for hard landing, max_cas_kias for VMO, max_cas_gear_down for VLE, max_apu_egt for APU EGT), AMM limit (reuse the module constants ITT_TAKEOFF_C / N2_VIB_AMBER / VMO_KIAS etc.; for hard landing use the per-row hard_landing_g_limit, for VLE use VLE 265 kias as a local constant), and Recommended action (static AMM-task strings already used elsewhere on the page, e.g. APU 'MPP7166_05-50-55', N2 vib 'borescope per AMM', others a generic 'review ATA 05 task'). Guard each parameter with df.get(...)/column presence and skip entirely-absent ones; sort events most-recent first; show a count + most-recent-date st.metric pair and an st.success polite empty state when the selected tail has zero exceedances. Reuse df_full for the selectbox options is NOT needed here (this is exploratory drill-down, the sidebar filter is acceptable). All text in English; no data invented.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: Tier 2 is at 50% and the only un-touched-recently pages are FOQA (Jun-03) and Combustivel (Jun-04); FOQA is the cleaner, lower-risk target because Combustivel's cruise-burn normalization fix depends on duration/distance columns whose existence I cannot confirm without risking invented data. 'Drill-down por aeronave em alerta' is an explicit open Tier-2 backlog item and the heatmap added on Jun-03 creates a hotspot the operator currently cannot click into. This turns observation into action by pairing each event with its AMM limit and a recommended task — satisfying 'Ação > observação' and 'Contexto sempre visível (MSN, data, limite AMM)'. It is purely flag-filtering over existing columns (no fragile statistics, no slider-dependent baselines), avoiding the statistical-fragility traps from the last two runs, and the per-event AMM-limit + action context is not duplicated by the raw Event Log tab.
- Self-eval: alignment=7, value=6, quality=8, risk=2

## 2026-06-07 07:00
- **[Tier 2]** Compute the SAV failure alerts over the full unfiltered fleet instead of the sidebar-filtered df. After dtype normalization, build df_lh_full/df_rh_full (drop NaN dates + sort, NO MSN/days_back filter). Change the two '🔴 In alert' KPI cards to count from the full fleet. Add a fleet-wide safety triage banner immediately under the KPI row: st.error naming the specific MSNs whose latest flight predicts pre-failure on LH and/or RH (e.g. 'LH pre-failure predicted: MSN 123, MSN 456 — inspect starter air valve per AMM'), with a caption noting it is fleet-wide and ignores the sidebar filter; st.success when none. Also switch the 'Current Risk Status' tab to use df_lh_full/df_rh_full so no aircraft in alert can be hidden by the cosmetic filter. Trend-chart tabs stay on the filtered df (exploratory). All text in English; no data invented; guard empty/missing columns.
- Files: `dashboard/pages/1_SAV.py`
- Rationale: Applies the most-repeated, highest-value lesson across recent runs ('safety/no-dispatch alerts must be computed over the whole fleet, never inherit cosmetic UI filters') to the one safety-critical page that still violates it, plus the 'expose the specific MSNs, not just a count' lesson — turning an aggregate number into a named, actionable triage. The page was not touched in the last 3 runs, the change is isolated and low-risk, and it advances Tier 2's open 'drill-down/alert per aircraft' item without cosmetic polish.
- Self-eval: alignment=8, value=7, quality=6, risk=3

## 2026-06-08 07:00
- **[Tier 2]** Normalize Section 5 (Per-Aircraft Cruise-Burn Degradation vs Own Baseline) by cruise duration: replace the absolute _cruise_total (kg) signal with a cruise specific burn rate (kg/h) = _cruise_total / (time_sec_cruise/3600). Build a guarded derived column df['_cruise_kg_per_hr'] (only where time_sec_cruise is present and > 0; drop rows with missing/zero duration for this section). Keep the identical baseline/amber(+5%)/red(+10%) logic, the rect bands, the three hlines, the per-flight marker coloring, the recent-N vs baseline verdict banner and the empty/low-sample/non-positive guards — only swap the metric to the rate and update units/labels (y-axis 'Cruise fuel rate (kg/h)', baseline annotation, banner text 'burning X% above its own cruise burn-rate baseline'). Update the section caption to state the signal is normalized by cruise time so route length no longer confounds engine health. If time_sec_cruise column is entirely absent, fall back to the existing absolute-kg behavior with a caption note. All text in English; no data invented.
- Files: `dashboard/pages/4_Combustivel.py`
- Rationale: Section 5 is the only part of the page that names a specific MSN and recommends 'inspect for engine degradation', yet it compares absolute cruise kg — a longer cruise (longer route) burns more fuel regardless of engine health, the exact false-positive trap documented in the lessons ('normalizar a queima por duração ou distância do cruzeiro antes de comparar voos; kg absolutos confundem comprimento de rota com saúde do motor'). The required input (time_sec_cruise) is confirmed present in the dashboard parquet, so this is the highest-integrity, highest-value fix available: it makes a tail-flagging safety/maintenance signal actually trustworthy instead of polishing exploratory charts. The page was not touched in the last 3 runs, satisfying rotation rules.
- Self-eval: alignment=8, value=8, quality=8, risk=3

## 2026-06-09 07:00
- **[Tier 3]** Add an 'Upcoming Maintenance Forecast' section to the Fleet Overview (Tier 3 'próximas inspeções'), inserted after the Immediate Actions / Watch-list banners and before the Fleet Health Matrix divider. For each E2 aircraft, reuse the proven oxygen-pressure dispatch-forecast method but harden it per the robustness lessons: restrict to flights within the sidebar days_back window (recency guard), require >=4 flights in-window, derive current_psi from the median of the last 5 readings (no single-reading alarm) and daily_drop from the MEDIAN of delta_press (not mean), skip non-positive drop. Project days_to_amber = (current_psi - 845) / daily_drop and an estimated calendar date. Render an st.warning listing the specific tails forecast to cross the 845 PSI amber (no-dispatch) threshold within a fixed 30-day planning horizon, each naming the aircraft (via _dnm), the estimated servicing date, days remaining, the PSI path and the ATA 35 task; st.success when none are due; st.info when oxygen-pressure history is insufficient or columns absent. Computation is fleet-wide (no MSN filter exists on this page) and fully guarded for empty/missing oxy data and absent psi/delta_press columns. All text in English; no data invented.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: Tier 1 (90%) and Tier 2 (80%) are saturated, so Tier 3 is unblocked. The previous run no-op'd because the commercial KPI it planned was already present; the only remaining Tier 3 open item not yet built is 'próximas inspeções' (upcoming inspections). The existing Immediate Actions banner only surfaces what is ALREADY critical — it does not tell a planner what will become critical. A forward-looking maintenance calendar is exactly the executive/MRO-facing value a manager notices in a demo and directly advances Tier 3. It reuses an already-validated forecast method (3_Oxigenio.py Section 5) but applies the documented robustness lessons (median drop + smoothed current reading + minimum sample + recency window) to avoid the single-reading false-positive trap, and computes fleet-wide so no aircraft is hidden by a cosmetic filter.
- Self-eval: alignment=7, value=6, quality=6, risk=3

## 2026-06-09 07:00
- **[Tier 3]** Fix two veracity defects in the forecast section I shipped last run, both flagged in self-evaluation and now verified against the producer op. (1) Correct the per-aircraft forecast line unit label at line 324 from 'PSI/flight' to 'PSI/day': save_oxy_report.py:73 sums delta_press over groupby(['aircraftSerNum-1','date']), so each value is one calendar day's total drop — the median daily_drop is genuinely per-day and the existing Timedelta(days=days_to_amber) projection is correct; only the label lied. (2) Rename the section title from '📅 Upcoming Maintenance Forecast' to '📅 Oxygen Servicing Forecast' so the heading reflects the real scope (oxygen/ATA 35 only), not a multi-system promise. Tighten the caption to say 'median daily PSI drop (PSI/day)' explicitly for unit clarity. No logic, math, guards, or computation changes — text-only edits inside the already-guarded block.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: The previous run introduced this section and my own self-evaluation flagged a unit/label inconsistency and an over-promising title as the top residual risks. Rather than stack a new feature on a freshly-shipped block whose labels are wrong, I verified the actual semantics against the canonical source (save_oxy_report.py) — confirming the math is right and the label is the bug — and fix exactly that. Leaving a wrong unit ('PSI/flight') and an oversold title in a paid MRO product erodes trust in the forecast date, which is the whole point of the feature. This directly applies the documented lessons on UI veracity and scope-honest section titles, and is a ~4-line, zero-logic-risk correction.
- Self-eval: alignment=6, value=5, quality=8, risk=1

## 2026-06-10 07:00
- **[Tier 3]** Add Wheels & Brakes (ATA 32) removal coverage to the executive Fleet Overview using a LONG-format-aware OR aggregation (NOT _latest, which collapses to one row and drops 5 of 6 positions). New helper _wnb_alerts(df_wnb, cutoff): detect ac_sn col; pred_cols = [c for c in df.columns if c.startswith('prediction_')]; return {} if empty/no pred_cols; filter date>=cutoff; take latest record per (ac_sn, position) via sort_values('date').groupby(['ac_sn','position']).last(); then agg = that.groupby('ac_sn')[pred_cols].max() (NaN-skipping = OR across positions); wnb_alert[msn] = 1 if float(row.max())==1 else 0, guarding all-NaN. Build wnb_alert dict, add its keys to all_e2. Surface flagged tails in the Immediate Actions banner ('MSN X — W&B: predicted brake/gear removal (inspect landing gear ATA 32)'). Add a 'W&B' column to the E2 Health Matrix (🔴 if ==1, 🟢 if msn in wnb_alert, — otherwise) and fold its cell into the per-row worst-status calc. Add wnb_catch_msns = {msn where wnb_alert==1} to the predictive_catches union (len(sav_catch | oxy_nodispatch | wnb_catch) + airbus_catch) and extend the c6 metric help text to mention brake/gear removal. All text English; binary prediction columns only, no thresholds invented.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: Closes the exact safety false-negative the last run shipped-then-reverted: the executive overview currently omits landing-gear/brake removal predictions entirely. The fix is the documented lesson (long-format OR over all 6 positions instead of _latest), so this applies the most recent learning directly rather than repeating the failure pattern. Brake/gear removal coverage is high-visibility MRO value a maintenance manager notices in a demo, and Tier 3 (20%) is the active tier with no higher tier unlocked.
- Self-eval: alignment=8, value=8, quality=7, risk=3

## 2026-06-11 07:00
- **[Tier 2]** Close the exceedance-coverage gap in the FOQA safety triage surfaces: add ITT Continuous (itt_lh/rh_continuous_exceedance, value max_itt_lh/rh_climb, limit 1006°C) and Low Oil Pressure (oil_press_lh/rh_low, value min_oil_press_lh/rh, limit 50.3 psig) to both the Fleet Exceedance Heatmap (param_specs OR-aggregation + present_flag_cols set) and the Per-Aircraft Drill-Down (DRILLDOWN_SPECS rows with recommended actions 'review ATA 05 continuous limit' / 'check oil system per AMM'). All flags are already produced by foqa_compute_parameters.py and counted in any_exceedance; existing column-presence guards apply to the new specs.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: Last 5 runs all touched 0_Fleet_Overview.py, so rule 3 forces another page; Tier 3 open items are Fleet-Overview-only and thus blocked. FOQA is a safety tool and the documented lesson is to enumerate ALL producer flags — ITT continuous and low-oil-pressure exceedances are computed and feed any_exceedance yet are invisible per-tail (not in heatmap nor drill-down), so an aircraft flagged solely for them shows up nowhere. This is a genuine false-negative coverage gap, not cosmetic polish. Uses only constants already in the file (ITT_CONTINUOUS_C, OIL_PRESS_MIN); no thresholds invented.
- Self-eval: alignment=8, value=7, quality=8, risk=2

## 2026-06-12 07:00
- **[Tier 3]** Fold A320 starter-valve pre-failure catches (sav_a320_alerts, binary sav_failure_pred==1, already shown in the Immediate Actions banner) into the headline commercial KPI 'Unsched. removals flagged'. Build sav_a320_catch = {f'A320:{tail}' for tail in sav_a320_alerts} and union it with airbus_catch inside predictive_catches (len(... | sav_a320_catch)), so a single A320 tail flagged by both FOQA and SAV is still counted once. Extend the c6 metric help text to mention A320 starter-valve pre-failure as a catch type.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: predictive_catches is the headline commercial-value number (Tier 3 'manutenções não-programadas evitadas'). A320 SAV pre-failure predictions are a real catch category — same binary semantic as E2 SAV (lessons: model.predict 0/1 → float==1, no invented thresholds) — and are already surfaced in the banner, yet excluded from the KPI, silently undercounting the product's commercial story in a demo. Additive, no thresholds invented, ~6-line diff, fixes a verified inconsistency between banner and KPI without touching any other surface. Fuel was rejected because its parquet has no prediction/flag column, requiring an invented degradation threshold.
- Self-eval: alignment=7, value=6, quality=7, risk=4

## 2026-06-12 07:00
- **[Tier 3]** Add an 'A320 Fleet — Health Matrix' to the executive Fleet Overview, shown only in the A320 iteration of the existing Airbus loop (A330 keeps bar-chart only, since it has no SAV signal — avoids a redundant re-presentation of the bar chart). One row per A320 tail combining its two monitored systems: a FOQA cell (reusing the file's already-established convention: 🔴 if >5 exceedance events, 🟡 if >0, 🟢 if 0, with the count shown) and a SAV cell (🔴 if the tail is in sav_a320_alerts i.e. sav_failure_pred==1 on any engine, 🟢 if the tail has SAV data but no alert, — if no SAV record). Build a new sav_a320_known set of normalized tails present in the SAV reports (from aircraft_id, str.strip()) alongside the existing sav_a320_alerts construction, and normalize sav_a320_alerts keys with .strip(). Match FOQA tail_number to SAV aircraft_id via str(tail).strip(); when no SAV record matches, fall back to '—' (never a false 🟢), per the documented serial-mismatch caution. Compute a per-row Overall worst status, sort critical-first, show top 10 with a small inline cell-coloring styler local to the block (do not depend on the E2-block _color_matrix, which may be undefined when E2 is filtered out). Add a caption stating the 🔴/🟡/🟢 meanings and that FOQA 🔴 = >5 events / SAV 🔴 = predicted starter-valve pre-failure. All text English; no thresholds invented (binary SAV prediction + the existing >5 FOQA red convention only); purely additive with column-presence/empty guards.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: Tier 3 (40%, not saturated) is the only non-saturated lower tier and its surface is this executive page; tier gating directs work here. The A320 fleet currently has its FOQA and starter-valve signals scattered (a count bar chart + banner lines) with no single per-tail triage view like the E2 Health Matrix — a real multi-fleet coverage gap that directly serves the product's core promise to Airbus operators. Unlike the recent count-reconciliation tweaks flagged as low-value in the lessons, this is a new actionable artifact a maintenance manager acts on. It explicitly avoids the prior W&B long-format trap (no _latest reuse) and the documented serial-mismatch trap (SAV degrades to — not a false 🟢; SAV pre-failure also remains independently surfaced in the banner, so no safety regression). The >5 FOQA red threshold is reused from existing code in the same file, not invented.
- Self-eval: alignment=6, value=6, quality=6, risk=3

## 2026-06-13 07:00
- **[Tier 2]** Add a directional indicator to the Per-Aircraft Exceedance Drill-Down so the operator sees WHY each value tripped. Extend DRILLDOWN_SPECS tuples with a 7th 'direction' field ('high' = exceed-above, 'low' = fall-below); default the existing specs to 'high' and mark the two Low Oil Press rows as 'low'. In build_drilldown_events add a 'Limit' column rendering '▲ above 1054' for high specs and '▼ below 50.3' for low specs (using the per-row AMM limit), so 'min_oil_press 45 / limit 50.3' is no longer ambiguous. Place the column right after 'Parameter'. Purely additive; preserves all column-presence/empty guards and the HARD_LANDING per-row limit logic. All text English.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: This is the concrete would_do_differently from the 2026-06-11 run and a documented lesson ('Para limites direcionais min-abaixo vs max-acima, a tabela de drill-down não comunica a direção; specs de parâmetros low devem carregar o operador de comparação até a UI'). On a paid safety-triage tool, an operator reading 'min_oil_press 45 / AMM limit 50.3' next to ITT '1060/1006' cannot tell that oil pressure trips by being BELOW the limit — the direction is decision-relevant context. The 5_FOQA_MOQA page was not touched in the last 2 runs (diversifies away from 0_Fleet_Overview, satisfying rule 3), and the change is small, additive, and low-risk.
- Self-eval: alignment=8, value=7, quality=6, risk=2

## 2026-06-13 07:00
- **[Tier 3]** Anchor the A320 Health Matrix (lines ~577-605) on the UNION of FOQA and SAV tails instead of iterating only _a320_foqa_counts. Change the loop to `for _tail in sorted(set(_a320_foqa_counts) | sav_a320_known):`, look up `_n = _a320_foqa_counts.get(str(_tail).strip())`; when the tail has a FOQA count apply the existing >5🔴/>0🟡/0🟢 convention, otherwise render the FOQA cell and its count as '—' (no false 🟢). Keep the SAV cell logic (🔴 in sav_a320_alerts / 🟢 in sav_a320_known / — otherwise), so a SAV-only pre-failure tail surfaces as Critical. Preserve the FOQA-display formatting (show count only when present), the Overall worst-status calc, top-10 critical-first sort, _color_hm styler, empty-state guard, and the existing str.strip matching.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: This removes a documented safety false-negative just introduced in the previous run: a tail predicted for starter-valve pre-failure (SAV 🔴) but with no FOQA record in the window currently never renders, hiding the single most critical case from the executive triage surface. The lessons mandate anchoring multi-system safety matrices on the UNION of all systems (FOQA ∪ SAV) and treat 'falling to no-alert' as a grave regression, not polish. Tier 3 is the active tier (50% < 80%, blocks Tier 5) and the only open items live on this page, so this is the highest-value move available while respecting the tier-order rules; it reveals previously-invisible critical aircraft rather than merely reconciling counts.
- Self-eval: alignment=7, value=6, quality=8, risk=2

## 2026-06-14 07:00
- **[Tier 3]** Surface E2 fuel-burn engine-degradation on the executive Fleet Overview — the fuel parquet is already loaded (line 31) and cleaned (lines 64-68) but never used. Add a cached `_fuel_alerts(df_fuel, cutoff_ts)` helper that REUSES 4_Fuel.py Section-5's validated own-baseline method: per ac_sn, total cruise burn = sum of `cruise*fuelMeterFuelBurn*Kg` cols, normalized to kg/h via `time_sec_cruise` when present (else absolute kg); baseline = median of each tail's earliest ceil(30%)/≥5 in-window flights; recent = mean of last 5; level 2 if recent>baseline×1.10, 1 if >×1.05, else 0; require ≥5 flights and baseline>0; guard missing cols/empty df. Return {ac_sn: level} + {ac_sn: pct_above}. Build `fuel_alert`/`fuel_pct` over the full fleet within the window, add its keys to `all_e2`, add a 'Fuel' cell (🔴/🟡/🟢/—) to the E2 Health Matrix folded into the worst-status calc and display_cols, add red→Immediate Actions and amber→Monitor banner items ('MSN X — Fuel: +Y% vs own cruise-burn baseline — inspect engine degradation (ATA 73/76)'), fold red fuel catches (fuel_catch_msns) into the predictive_catches union, and update the c6 metric help text plus the page docstring 'Covers' line to include Fuel.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: Tier 3 is at 60% and lives on the executive page, so it must be advanced before Tier 5. Unlike the last three page-0 runs (which repositioned existing counts and even self-inflicted a regression — penalized as churn), this reveals genuinely new actionable information: a monitored system that was loaded but completely invisible on the overview. It reuses the already-validated kg/h own-baseline method, directly avoiding the documented fuel-normalization and fragile-polyfit (≥5 flights, no fleet-relative threshold) traps, and uses the same ac_sn/clean_df path as the existing E2 systems so no new serial-mismatch risk is introduced.
- Self-eval: alignment=7, value=6, quality=6, risk=3

## 2026-06-14 07:00
- **[Tier 3]** Make the A320 SAV fleet safety-triage banner (lines 113-124) recency-aware and add last-start age context per flagged tail, without dropping any alert. Add RECENCY_DAYS = 30 and a helper `_alert_tails_aged(df)` that returns {tail: age_days} for tails whose latest start predicts pre-failure (reuse the existing `df.sort_values('date').groupby(AC_COL).last()` pattern, take `latest['date']`, age_days = (pd.Timestamp.now().normalize() - latest_date.normalize()).days). Build aged dicts from df_e1_full/df_e2_full, then split each into active (age <= 30) and stale (age > 30). Banner logic: if any active → st.error red 'Fleet safety triage' listing active tails as '<tail> (last start <n>d ago) — inspect starter air valve (ATA 80)'; if any stale → st.warning amber 'Flagged but no start in 30+ days — likely grounded/in maintenance, verify aircraft status: <tail> (last start <n>d ago)'; if neither → keep the existing st.success. Keep the KPI metrics (c1-c4, lines 107-111) and `_alert_tails` UNCHANGED so the headline counts and the Current Risk Status tab stay consistent; update the banner caption (line 124) to note that red = predicted pre-failure on a recent start, amber = stale prediction pending status verification. All text English; purely additive to banner logic with empty/missing-column guards preserved.
- Files: `dashboard/pages/7_SAV_A320.py`
- Rationale: The banner is fed by df_*_full which has no date cutoff, so a pre-failure prediction from a grounded/in-maintenance aircraft's last start (weeks or months ago) renders forever as an urgent red 'inspect now' alert — exactly the false-permanent-alert pattern flagged in the lessons. An in-service A320 starts engines several times daily, so 30 days with zero starts reliably means the aircraft is out of normal service and its last prediction is no longer an actionable in-service signal (defensible operationally, not a magic number). The fix preserves the no-false-negative safety invariant (stale alerts are downgraded to amber 'verify status', never dropped), adds 'last start Nd ago' so the operator knows whether to act now or check aircraft status, and diversifies away from 0_Fleet_Overview (target of 4 of the last 5 runs). The same guard should next be applied to 1_SAV.py (E2) for cross-page consistency.
- Self-eval: alignment=8, value=7, quality=7, risk=3

## 2026-06-15 07:00
- **[Tier 5]** Append a new section '7. PSI Depletion Forecast — Uncertainty Cone' at the end of 3_Oxygen.py (after the Section 6 Life Analysis block, ~line 605), implementing Tier 5 'degradation curve with confidence interval' for crew-oxygen PSI depletion. Guard: build only when 'psi','delta_press','date' and AC_COL are present and df is non-empty, else st.info empty state. For each aircraft, on df dropna(['psi','delta_press','date']).sort_values('date'): current_psi = grp['psi'].tail(5).median() (reuse the robust median-of-last-5 convention from Section 5); leak = grp.loc[grp['delta_press']>0,'delta_press']; require len(leak)>=6 AND current_psi>PSI_AMBER else skip the tail (gate to avoid fragile quantiles). Compute slow=leak.quantile(0.25), med=leak.quantile(0.50), fast=leak.quantile(0.75) (PSI/day). days_x = max(0,(current_psi-PSI_AMBER)/rate) for each (guard rate>0); earliest_date = today+fast (pessimistic), latest_date = today+slow (optimistic), expected_date = today+med, where today=pd.Timestamp.now().normalize(). Build a dict of qualifying tails and pick the default in a st.selectbox = the tail with the smallest earliest days (most urgent), format_func via display_name/DISP_COL. For the chosen tail render a go.Figure: (a) its last ~15 actual PSI readings as a gray line+markers (date vs psi) for context; (b) three forward rays from (today,current_psi) to (crossing_date,PSI_AMBER): optimistic (slow leak), expected (median), pessimistic (fast leak); (c) a shaded uncertainty band between the optimistic and pessimistic rays using two go.Scatter traces with fill='tonexty' (light amber/red rgba); (d) add_hline at PSI_AMBER (red dash, 'No dispatch — 845 PSI') and PSI_CYAN (amber dot, 'Observer — 1155 PSI'); xaxis tickformat '%d-%b-%y', yaxis range [0, max(current_psi, PSI_CYAN)*1.1]. Below the chart, st.warning if earliest within ~30 days else st.info: 'MSN <display> forecast to cross the 845 PSI no-dispatch threshold between <earliest dd-Mon-YYYY> and <latest dd-Mon-YYYY> (expected <dd-Mon-YYYY>), from the P25–P75 spread of its own daily leak rate; projection assumes no recharge.' Honest labeling: caption describes the band as an uncertainty envelope from the P25–P75 spread of the aircraft's own daily leak rate — NOT a 95% CI. All text English; no new imports (pd, go, st already imported); purely additive with divide-by-zero, min-readings and column-presence guards; existing sections untouched.
- Files: `dashboard/pages/3_Oxygen.py`
- Rationale: Tiers 1–3 are saturated and Tier 4 is blocked, so Tier 5 (0%) is the correct frontier — anything in a lower tier would be churn. Among Tier 5 items, a degradation curve with a confidence interval is the most genuinely new predictive capability. Crew-oxygen PSI is the cleanest physical degradation signal available (a real, monotonic leak rate against the hard 845 PSI AMM limit), unlike the fuel signal the lessons flag as weak/advisory. The existing dispatch forecast gives only a point ETA with no uncertainty; a risk-based maintenance planner needs the earliest/latest crossing window. Deriving the band from empirical P25–P75 of the aircraft's own daily-leak distribution is statistically robust and sidesteps the fragile-polyfit pitfall the lessons warn about, while reusing existing psi/delta_press columns (sign convention confirmed at lines 411–413). 3_Oxygen.py avoids the last-3 pages rule (0_Fleet_Overview, 7_SAV_A320) and carries no documented failure pattern.
- Self-eval: alignment=7, value=6, quality=6, risk=2

## 2026-06-15 07:00
- **[Tier 5]** Add a new tab 'Fleet Degradation Ranking' (alongside the existing LH/RH/Risk/EDA tabs) implementing Tier 5 'comparative degradation between aircraft'. Add an LH/RH radio (reuse the EDA pattern). For the chosen side: (1) reuse _signal_eda over that side's df_full to get each signal's normal-class distribution and keep ONLY confirming signals (median_alert beyond normal P75 for bad_dir 'up' / P25 for 'down'); if none confirm or distribution unavailable, st.info empty state. (2) For each aircraft in df_full, compute the recent median of its last 5 flights per confirming signal (groupby ac_sn over the long per-flight data, using _resolve_col + SIGNALS columns), requiring >=3 non-null recent readings else leave that cell NaN. (3) Convert each recent median to a DIRECTIONAL degradation percentile vs the normal distribution: bad_dir 'up' -> pct = share of normal flights below the recent median x100; bad_dir 'down' -> pct = share of normal flights above it x100, so higher pct = more degraded regardless of direction. (4) Render one go.Heatmap, rows=aircraft (display_name prefix+MSN), cols=confirming signals, z=degradation pct with FIXED zmin=0/zmax=100, RdYlGn_r colorscale, per-cell text annotation (rounded pct, NaN blank); sort rows by composite score (mean pct across confirming signals) descending, cap at top 12. (5) Below, a ranked dataframe: display_name, composite degradation score, count of signals in the red zone (pct>=75), and a 'model alert' marker cross-referencing ALERT_HISTORY_LH/RH to tie the continuous ranking back to the binary model. Caption: continuous early-warning degradation comparison vs the fleet's own normal distribution that complements (not replaces) the binary Current Risk Status; computed fleet-wide ignoring the sidebar filter. All text English; reuse _signal_eda/_resolve_col/_dnm/SIGNALS/ALERT_HISTORY_*; no new imports; purely additive with min-flights, column-presence and empty-distribution guards; existing tabs untouched.
- Files: `dashboard/pages/1_SAV.py`
- Rationale: Tier 5 is the active frontier (Tiers 1-3 saturated, Tier 4 blocked) and the last Tier 5 run touched 3_Oxygen, so this diversifies to a page outside the last 3 runs. 1_SAV.py is the strongest base: it carries a TRAINED, validated model and an existing EDA that already computes the normal-class distribution per confirming signal, so the ranking reuses empirical percentiles instead of invented thresholds. It delivers a genuinely new capability (continuous, sub-threshold degradation comparison across aircraft) rather than duplicating the binary 'Current Risk Status' or the population-level EDA. It honors prior lessons: directional percentile inverts 'down' signals, the heatmap fixes zmin/zmax and annotates cells (triage-correct, not auto-normalized), fragile stats are gated by a >=3 recent-flight minimum and confirming-signals-only, top-12 by risk with prefix+MSN, and it touches no commercial KPI so no unvalidated heuristic is folded into predictive_catches.
- Self-eval: alignment=7, value=6, quality=6, risk=2

## 2026-06-16 07:00
- **[Tier 5]** Append a new self-contained 'Section 6: Model Track Record — Predicted vs Confirmed Removals' after the Section 5 Life Analysis block (after line 636), implementing the untouched Tier 5 'prediction history vs reality per component' for W&B. Add a cached helper `@st.cache_data def _load_track_record():` that (a) reloads the FULL unfiltered report via `load('e2_wnb_report.parquet')` (never the page's cosmetically filtered `df`), parses `date` with `pd.to_datetime(errors='coerce')`, coalesces the six prediction columns into one per-flight flag via `pred_cols=[c for c in df.columns if c.startswith('prediction_')]; df['_any_pred']=df[pred_cols].max(axis=1).fillna(0)` (row-wise OR — the documented failure-pattern fix, since only the matching position column is non-null per row), and normalizes serials with `_key=lambda s: str(s).split('.')[0].strip()[-5:]`; (b) loads `e2_wnb_maintenance.parquet` and `e2_brake_maintenance.parquet`, tagging each with Component='Wheel'/'Brake', concatenating only sources whose required columns (AC_SN, POSITION, TRANSACTION_DATE) are present, parsing TRANSACTION_DATE and normalizing AC_SN with the same `_key`. Set LEAD_WINDOW_DAYS = 90. For each removal row, subset the report to the same normalized serial within [TRANSACTION_DATE - 90d, TRANSACTION_DATE]: if zero report flights in window -> status '⚪ No telemetry in window' (excluded from rate); else flagged = window rows with _any_pred==1, caught = len(flagged)>0, status '✅ Caught'/'❌ Missed', lead_days = (TRANSACTION_DATE - flagged['date'].min()).days when caught. Render: an empty-state st.info if either source is empty/missing columns OR the join produces zero evaluable removals (NEVER fabricate); otherwise a KPI row via st.columns(4) — Removals evaluated (N, telemetry-covered only), Caught in advance (k), Catch rate (k/N as {:.0f}%), Median lead time (days, from caught only) — plus a sortable st.dataframe with columns: MSN (display_name(_key, _prefix_map) prefix+MSN), Component, Position, Removal date (%d-%b-%Y), Status, Lead time (days), Removal reason; sort by removal date descending, cap at the 30 most recent. Caption (English): retrospective track record over the period the prediction report covers, aircraft-level OR aggregation across all six gear positions within a 90-day lead window; legitimate model validation (per-position trained models cross-checked against TRAX removals), not a live alert; '⚪' rows lack telemetry coverage and are excluded from the catch rate. All text English; reuse the already-built `_prefix_map`, imported `display_name`/`load`/`pd`/`np`/`st`; no new imports; purely additive with column-presence, empty-df, divide-by-zero and zero-match guards; existing sections untouched.
- Files: `dashboard/pages/2_Wheels_and_Brakes.py`
- Rationale: This is the only untouched Tier 5 item ('histórico de predições vs. realidade por componente') and W&B is the single non-blocked page (last 3 runs hit 7_SAV_A320, 3_Oxygen, 1_SAV) that loads BOTH trained-model predictions and loadable ground-truth removals (e2_wnb/brake_maintenance.parquet with TRANSACTION_DATE). It directly answers the most-repeated lesson — 'toda métrica de early-warning deve ser cruzada com falhas confirmadas antes de virar visual' — turning W&B's contribution to the commercial predictive_catches KPI from a claim into a demonstrated track record (catch rate + median lead time vs real TRAX removals), exactly the proof a paying maintenance manager notices. It mitigates this page's documented long-layout failure by using row-wise max OR aggregation and an aircraft-level join (sidestepping fragile maintenance-POSITION ↔ report-position mapping), and degrades gracefully via strict empty-state and zero-match guards instead of inventing data.
- Self-eval: alignment=6, value=5, quality=6, risk=4

## 2026-06-17 07:00
- **[Tier 5]** Append a new section 'N2 Vibration Degradation Forecast' inside the Engine Trends tab (tab_eng), right after the existing N2 Vibration scatter block (after line 388), implementing the open Tier 5 item 'degradation curve with confidence interval' — done correctly this time, anchored in the CERTIFIED AMM amber limit (N2_VIB_AMBER = 4.00 AU) instead of an invented heuristic, and with the uncertainty band derived from the regression fit's confidence interval rather than daily-volatility quantiles (the explicit fix for the 3_Oxygen.py run criticism). Add `import numpy as np` to the imports (only new import; numpy is a project dependency) and add `display_name` to the existing `from utils.drive_loader import ...` line; reuse the already-built `_foqa_prefix_map`, `df_full`, `ac_col` and the N2_VIB_AMBER/N2_VIB_BORESCOPE/N2_VIB_REMOVE constants. Add a cached helper `@st.cache_data(ttl=300) def _n2vib_forecast(df_in, ac_col):` that, for each engine side ('LH'->'max_n2_vib_lh', 'RH'->'max_n2_vib_rh') present in the columns, groups df_full by ac_col, on each group does dropna([param,'date']).sort_values('date'), and requires >=6 dated flights else skips (anti-fragile gate per the polyfit lesson). Build x = (date - date.min()).days as float, y = param. Fit OLS via np.polyfit(x,y,1) -> slope (AU/day), intercept; compute R^2 = 1 - SSE/SST and skip the engine if R2 < 0.30 OR slope <= 0 (only forecast coherent UPWARD trends; abrupt FOD/blade step-changes give low R2 and are intentionally excluded — they are already caught by the exceedance flags). current_fit = np.polyval([slope,intercept], x.max()) (anchor the present value on the trend, not a raw reading, per the lesson). Skip if current_fit >= N2_VIB_AMBER (already in exceedance — handled by other sections). Compute residual_std = sqrt(SSE/(n-2)), Sxx = sum((x-x.mean())**2), slope_se = residual_std/sqrt(Sxx); use t≈2.0 (n-2 dof, simple constant, no scipy) so slope_hi = slope + 2*slope_se, slope_lo = max(slope - 2*slope_se, 1e-9). days_to_amber = (N2_VIB_AMBER - current_fit)/slope; earliest = (AMBER-current_fit)/slope_hi (steepest), latest = (AMBER-current_fit)/slope_lo (shallowest, cap/None if slope_lo<=1e-9 i.e. CI includes zero -> 'trend not significant'). today = pd.Timestamp.now().normalize(); expected/earliest/latest cross dates = today + the respective days. Return a list of dicts per qualifying engine {ac_sn, side, n, r2, current_fit, slope_per_month (slope*30), days_to_amber, earliest_date, expected_date, latest_date}. RENDER: st.subheader('🔮 N2 Vibration Degradation Forecast') + caption explaining this is an advisory engine-condition trend (ECTM) toward the certified 4.00 AU amber limit, the band is the 95% confidence interval of the linear trend fit (NOT a daily-drop quantile and NOT a per-flight alert), gated to engines with >=6 flights and R^2>=0.30, and that abrupt vibration jumps are caught by the exceedance flags not this trend. If the helper returns no engines -> st.success('✅ No engine shows a significant upward N2 vibration trend — all fits are flat or improving.'). Otherwise: (1) a fleet COMPARATIVE ranking st.dataframe sorted by days_to_amber ascending (this also advances the 'comparative degradation between aircraft' Tier 5 item) with columns Aircraft (display_name(ac_sn,_foqa_prefix_map)), Engine (side), Flights (n), Trend (AU/month, slope_per_month {:+.3f}), Fit R² ({:.2f}), Current N2 vib ({:.2f} AU), Expected amber-cross ({:%d-%b-%Y}), capped at top 12 most urgent; (2) a st.selectbox (default index = the most urgent engine i.e. smallest days_to_amber, format via display_name+side) and for the chosen engine a go.Figure: its raw N2 vib points as gray markers (date vs param), the fitted trend line extended forward from first flight to the expected amber-cross date (or +180d, whichever sooner-but-show-line), two go.Scatter traces forming the confidence band ŷ(x)±2*residual_std*sqrt(1/n+(x-x̄)²/Sxx) with fill='tonexty' light-amber rgba (band widens on extrapolation — honest forecast uncertainty), add_hline at N2_VIB_AMBER (orange dash '4.00 AU amber'), N2_VIB_BORESCOPE (red dash '4.08 AU borescope'), N2_VIB_REMOVE (darkred '4.31 AU remove'); xaxis tickformat '%d-%b-%y', height 340. Below the chart st.warning if expected cross within 180 days else st.info: 'MSN <display> <side> engine N2 vibration trending toward the 4.00 AU amber limit — expected crossing <expected dd-Mon-YYYY> (95% CI <earliest> to <latest>), R²=<..> over <n> flights. Advisory trend; schedule a borescope per AMM before the amber crossing.' All text English; purely additive with column-presence, n>=6, divide-by-zero, slope<=0, R²<0.30, NaN and empty-df guards; every existing section/tab untouched.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: Tier 5 is the lowest-progress unblocked tier (30%) and its 'degradation curve with confidence interval' item was only attempted on 3_Oxygen.py, where the self-eval flagged two real defects: (a) using the P75 of the daily drop as a permanent rate over-states urgency, and (b) the signal was an unvalidated own-baseline heuristic. Both are structurally avoided here: the target is the CERTIFIED AMM amber limit (4.00 AU) — exceeding it is a regulatory event, so this is the most defensible 'ground truth' available without leaving dashboard/, directly answering the repeated lesson that early-warning visuals need validation against confirmed/limit-based failure rather than invented thresholds; and the uncertainty band is the linear-fit's confidence interval (widening on extrapolation) instead of daily-volatility quantiles. Statistical fragility is gated per the polyfit lesson (≥6 flights, R²≥0.30, positive slope, trend-anchored current value). The page (5_FOQA_MOQA.py) was not touched in any of the last 5 runs, satisfying diversification, and it already has the N2 vib data, constants and df_full loaded but exposes them only as exploratory scatter — adding the forecast + fleet ranking turns latent data into an actionable 'when does this engine need a borescope' answer a maintenance manager would notice in a demo.
- Self-eval: alignment=8, value=7, quality=6, risk=2

## 2026-06-17 07:00
- **[Tier 5]** Add a 5th tab 'Fleet Degradation Ranking' (alongside Engine 1 / Engine 2 / Current Risk Status / Threshold Analysis) implementing the Tier 5 'comparative degradation between aircraft' item for the A320/A321 SAV fleet — the same continuous early-warning ranking already shipped on 1_SAV.py (run 2026-06-15T16), but built with the documented fix from that run's self-evaluation. Extend the existing `st.tabs([...])` call (line 298) to unpack a new `tab_rank`; all new code goes in a new `with tab_rank:` block appended after the `tab_eda` block (after line 408). Add an Engine 1/Engine 2 radio reusing the EDA pattern (`key='ab_rank'`) and define MIN_RANK_SIGNALS = 2. For the chosen side's full unfiltered frame (df_e1_full / df_e2_full, never the sidebar-filtered df): (1) call the existing `_signal_eda(df_full, col, PRED_COL)` over each entry of `SIGNALS` and keep ONLY confirming signals — `median_alert > p75_normal` for bad_dir 'up', `median_alert < p25_normal` for 'down'; if none confirm or distributions are unavailable, render an `st.info` empty state and stop. (2) For each aircraft (`groupby(AC_COL)` over the long per-start data, sorted by date), compute the recent median of its last 5 starts per confirming signal, requiring >=3 non-null recent readings else leave that cell NaN. (3) Convert each recent median to a DIRECTIONAL degradation percentile vs that signal's normal distribution from `_signal_eda` (`stats['normal']`): bad_dir 'up' -> `pct = float((normal < val).mean())*100` (share of normal starts below the recent median); 'down' -> `pct = float((normal > val).mean())*100`; higher pct = more degraded regardless of direction (pure pandas, no numpy import). (4) THE FIX (from the 1_SAV.py lesson): compute the composite score as the mean pct across confirming signals ONLY for aircraft whose count of non-null confirming-signal cells is >= MIN_RANK_SIGNALS; aircraft below that coverage are excluded from the ranked heatmap/table (surfaced in a small `st.caption` listing how many were dropped for thin coverage) so a single high signal can never top the ranking. (5) Render one `go.Heatmap`: rows = aircraft (raw aircraft_id, which is already the tail), cols = confirming signals, z = degradation pct with FIXED zmin=0/zmax=100, RdYlGn_r colorscale, per-cell text annotation (rounded pct, NaN blank), rows sorted by composite descending, capped at top 12. (6) Below it a ranked `st.dataframe`: Aircraft (aircraft_id), Composite degradation score, Signals with data (coverage count), Signals in red zone (pct>=75), and a 'Model alert' marker cross-referencing ALERT_HISTORY_E1/ALERT_HISTORY_E2 to tie the continuous ranking back to the binary model; sort by composite descending. Caption (English): continuous early-warning degradation comparison against the fleet's own normal start distribution that COMPLEMENTS (does not replace) the binary Current Risk Status; computed fleet-wide ignoring the sidebar filter; aircraft with fewer than 2 confirming signals with data are excluded to keep the composite comparable; this is a relative heuristic, not a model validated against confirmed SAV removals. All text English; reuse `_signal_eda`/`SIGNALS`/`ALERT_HISTORY_E1`/`ALERT_HISTORY_E2`/`AC_COL`/`PRED_COL`; no new imports (pd, px, go, st already imported); purely additive with empty-distribution, min-flights, column-presence and divide-by-zero guards; every existing tab/section untouched.
- Files: `dashboard/pages/7_SAV_A320.py`
- Rationale: Tier 5 (40%) is the only unsaturated, unblocked tier and 'comparativo de degradação entre aeronaves' is an open item; the E2 SAV page already has this ranking but the A320/A321 SAV page — a separate paying fleet — does not, so this propagates an accepted feature for cross-fleet consistency. Crucially it ships the explicit fix the 1_SAV.py run flagged in its own self-evaluation (require a minimum number of non-null confirming signals before the composite, otherwise skipna-mean over a single signal distorts the ranking), turning a documented weakness into a correct delivery. It uses only empirical normal-distribution percentiles (no invented AMM threshold — avoiding the trap that blocks an Airbus FOQA forecast, since LEAP-1A vibration/EGT limits are not in context), reuses the page's existing `_signal_eda`/`SIGNALS`/`ALERT_HISTORY` helpers and is purely additive (new tab). 7_SAV_A320.py was last touched 5 runs ago and is outside the last-3-run window, so it diversifies the target.
- Self-eval: alignment=6, value=6, quality=7, risk=2

## 2026-06-18 07:00
- **[Tier 5]** Add a new tab 'Confirmed Failures (ACARS-validated)' to the existing st.tabs(...) on 1_SAV.py that surfaces the project's ground-truth validation table e2_sav_confirmed_failures (TRAX SAV removals cross-referenced with FHDB FIM fault codes) instead of yet another percentile heuristic. Add a cached helper (@st.cache_data) that opens the page's existing Postgres connection, introspects the table's columns, and SELECTs the removal record, wrapped in try/except: on missing table, connection error, or zero rows -> st.info empty state (never fabricate). Map columns defensively to the documented schema (tail/AC=matricula, engine/side LH-RH, removal date, confirmed bool, lead_days, fault_data_available, matched fault code). Normalize the serial with the page's existing key convention before resolving display_name. RENDER: (1) a KPI row st.columns(4) computed ONLY over rows with fault_data_available==True (removals in the discontinuous-coverage gap are excluded, never counted as NFF) — 'Removals with fault coverage' (N, shown prominently), 'Confirmed by ACARS' (k), 'Confirmation rate' (k/N as {:.0f}%, suppressed/marked '—' when N<5 to avoid misleading small-denominator %), 'Median lead time' (days, confirmed rows only); (2) a sortable st.dataframe — MSN (display_name prefix+MSN via _dnm/_prefix_map, never raw id), Engine, Removal date (%d-%b-%Y), Status (✅ Confirmed / ⚪ NFF candidate / — No fault coverage), Lead time (days), Matched fault code — sorted by removal date descending, capped at the 30 most recent. Honest English caption: ground-truth validation from TRAX removals × FHDB fault messages (FIM SAV fault codes 801103M*/710000M*/FDE 801 501 51-52), the SAME label the SAV models are evaluated against (GroupKFold AUC 0.74); fault coverage is discontinuous (gap 2024→2025) so removals without coverage are excluded from the rate. Reuse display_name/_prefix_map/_dnm/pd/st; add only the DB-read helper; purely additive with table-missing, empty-df, divide-by-zero and small-N guards; every existing tab/section untouched; <200 line diff.
- Files: `dashboard/pages/1_SAV.py`
- Rationale: The last five runs all stacked heuristic degradation lenses on the weak aggregated-telemetry SAV signal, and three consecutive self-evaluations recorded the SAME lesson: stop proliferating disclaimer-laden heuristics and instead VALIDATE existing predictions against the project's confirmed-removal label (e2_sav_confirmed_failures / FHDB). This change does exactly that — it advances the still-open, highest-value Tier 5 item 'prediction history vs reality' using the real ground-truth table rather than model self-reports, deliberately avoiding the in-sample leakage and aircraft-level OR pitfalls documented in the 2026-06-16 W&B failure pattern (N prominent, rate suppressed for small N, no in-sample model.predict). It is the most differentiated, commercially credible move available (a paying maintenance manager sees ACARS-confirmed removals with an 18.5-day median lead time), and 1_SAV.py was not touched in the last 3 runs (rule 3 satisfied).
- Self-eval: alignment=7, value=6, quality=5, risk=3

## 2026-06-19 07:00
- **[Tier 2]** Overlay certified FCOM/AMM limit lines + danger zones on the engine-trend scatter charts inside the '📈 Engine trends' expander (the `for col, title in trend_cols` loop, ~lines 214-230), so raw EGT/vibration/oil-pressure values gain triage context. (1) Add a module-level `_FLEET_LIMITS` dict mirroring ONLY the certified (non-placeholder) values from `_LIMITS` in repositories/azul/airbus_foqa_moqa_job/ops/airbus_foqa_compute_op.py — keyed 'A320FAM'/'A330': A320FAM {n1_vib_advisory:6.0, n1_vib_limit:5.0, n2_vib_advisory:4.3, n2_vib_limit:5.0, oil_press_advisory_psi:16.0, oil_press_red_psi:13.0}; A330 {n1_vib_advisory:5.7, n2_vib_advisory:5.6, oil_press_low_psi:30.0} — with a comment 'mirrors _LIMITS in airbus_foqa_compute_op.py (certified FCOM/AMM values); keep in sync. EGT limits intentionally omitted — producer marks them placeholder/TBD pending CFM/RR OEM data.' (2) Add a `_FLEET_KEY = {'A320/A321': 'A320FAM', 'A330': 'A330'}` map and a `_TREND_LIMITS` map from dashboard column -> list of (limit_key, severity['amber'|'red'], direction['up'|'down'], label): n1_vib_max -> [(n1_vib_advisory,'amber','up','N1 vib advisory'), (n1_vib_limit,'red','up','N1 vib AMM limit')]; n2_vib_max -> [(n2_vib_advisory,'amber','up','N2 vib advisory'), (n2_vib_limit,'red','up','N2 vib AMM limit')]; oil_press_min_1_psi and oil_press_min_2_psi -> [(oil_press_advisory_psi,'amber','down','Oil press advisory'), (oil_press_red_psi,'red','down','Oil press red'), (oil_press_low_psi,'red','down','Oil press low — ENG shutdown')]; egt1_max_c/egt2_max_c -> no entry (no line). (3) Add a small helper `_add_limit_overlays(fig, fleet_key, col, ymin, ymax)` that: looks up `_FLEET_LIMITS.get(fleet_key, {})` and `_TREND_LIMITS.get(col, [])`; for each (key,sev,direction,label) whose key is present and not None in that fleet's dict, add_hline at the value (amber='#d97706' dotted / red='#dc2626' dashed) with annotation_text=f'{label} ({value:g})'; then add ONE shaded danger rect using fig.add_hrect — for direction 'up' from the max present red value up to max(ymax, red*1.02); for 'down' from min(ymin, red*0.98) down to the min present red/low value — fillcolor light red rgba, layer='below', line_width=0; only draw the rect when at least one 'red' limit exists for that fleet+col. Guard everything: skip silently if fleet_key not in _FLEET_LIMITS, if col not in _TREND_LIMITS, or if no applicable keys present. (4) In the existing loop, after `fig_t = px.scatter(...)` and before `st.plotly_chart`, compute ymin/ymax from `df_plot[col]` (use np.nanmin/np.nanmax with a fallback guard) and call `_add_limit_overlays(fig_t, _FLEET_KEY.get(fleet_label), col, ymin, ymax)`. (5) Update the expander to add a one-line st.caption above the charts: 'Dashed/dotted lines mark certified FCOM/AMM advisory (amber) and maintenance/red limits; shaded band is the exceedance zone (above for vibration, below for oil pressure). EGT limits are omitted pending OEM (CFM/RR) confirmation.' Add `import numpy as np` (only new import; numpy is a project dependency). All text English; purely additive; existing summary/banner/table sections and the EGT charts (which simply get no overlay) untouched; every guard (column-presence via existing `if col not in sub.columns: continue`, empty df via existing `dropna`, missing-fleet, missing-key, NaN ymin/ymax) in place.
- Files: `dashboard/pages/8_Airbus_FOQA.py`
- Rationale: 8_Airbus_FOQA covers half the platform's fleet coverage (A320 + A330) yet its engine-trend charts plot raw EGT/vibration/oil values with no certified-limit reference — a documented Tier 2 gap ('8_Airbus_FOQA.py: sem linhas de threshold'). This surfaces the SAME certified limits the producer already uses to raise its exceedance flags, making those flags visually self-explanatory ('N2 vib 4.6 AU vs AMM limit 5.0'), which is actionable triage a maintenance manager notices in a demo. It deliberately breaks the recent anti-pattern flagged in the lessons (piling yet another weak percentile heuristic on the SAV signal): it adds NO new heuristic, only renders existing ground-truth thresholds. It is honest — the producer's EGT limits are marked placeholder/TBD, so they are excluded rather than rendered as certified — and direction-aware, respecting the lesson that 'low' parameters (oil pressure) need the danger zone below the line. The page is allowed (not among the last 3 runs: 1_SAV/7_SAV_A320/5_FOQA_MOQA) and carries no documented failure pattern.
- Self-eval: alignment=8, value=6, quality=5, risk=3

## 2026-06-19 07:00
- **[Tier 5]** Fix the two documented correctness defects in the N2 Vibration Degradation Forecast (helper _n2vib_forecast + its render block) so the safety-facing borescope projection is internally consistent and statistically honest. (1) DATE-ANCHOR: inside the per-engine loop compute last_flight_date = sub['date'].max() and anchor earliest_date/expected_date/latest_date on last_flight_date + Timedelta(days=...) instead of `today` (lines 204-206); this matches the chart, which already crosses amber at x0 + (x_max + days_to_amber) = last_flight_date + days_to_amber, and is the semantically correct anchor since current_fit/days_to_amber are evaluated at the LAST flight (x_max), not today. Drop the now-unused `today` from the helper. (2) CI MULTIPLIER: add a module-level pure-python helper `_t95(dof)` returning the two-sided 0.975 Student-t multiplier from a small lookup table (dof 1..30, e.g. 4->2.776, 5->2.571, 6->2.447, ...), falling back to ~2.04 for dof>30; in _n2vib_forecast compute t_mult = _t95(n - 2), use it for slope_hi = slope + t_mult*slope_se and slope_lo = max(slope - t_mult*slope_se, 1e-9), and store 't_mult' in the result dict; in the render block replace the hardcoded `band = 2.0 * residual_std * np.sqrt(...)` (line 530) with `f['t_mult'] * residual_std * np.sqrt(...)`, making the existing '95% CI' labels (caption line 476, trace name line 542, message line 574) truthful. (3) LOW-CONFIDENCE FLAG: keep the R²>=0.30 gate (do not exclude engines) but set low_conf = r2 < 0.50 in the helper; add a 'Confidence' column to the ranking table ('⚠ Low' if low_conf else 'OK') and append ' — low-confidence fit (R²<0.50), treat as indicative' to the advisory st.warning/st.info message when low_conf, per the documented lesson that R²>=0.30 is a weak floor to extrapolate to a regulatory borescope date. All text English; numpy already imported; purely surgical edits to the one helper and its render block with all existing guards (n>=6, slope>0, R²>=0.30, Sxx>0, NaN, divide-by-zero) intact; every other tab/section untouched; ~30-line diff.
- Files: `dashboard/pages/5_FOQA_MOQA.py`
- Rationale: The dominant, most-recent memory lesson explicitly says to stop stacking weak-signal heuristic lenses and instead make existing predictions honest/validated — this is exactly that: it corrects an existing Tier-5 forecast rather than adding a new lens. The confirmed-failures parquet-export path (the other validation lesson) is blocked by the dashboard-only constraint, but this defect is fully fixable here. It removes a real 'Veracidade de Fatos na UI' violation — text/table say one borescope date while the chart shows another, and a falsely-narrow band labeled '95% CI' understates uncertainty — both directly damaging to a paying maintenance manager's trust in a safety forecast. It is the agent's own would_do_differently from the 06-17 run, surgical and low-risk, and the Oxygen page already covers the 'degradation curve with CI' item (cone + Weibull) so adding there would be churn.
- Self-eval: alignment=7, value=6, quality=7, risk=2

## 2026-06-20 07:00
- **[Tier 5]** Add a new prominent 'Cruise-Burn Watchlist — Aircraft Above Their Own Baseline' section to 4_Fuel.py that exposes the specific MSNs behind the existing c4 'rising cruise-burn trend' count (which today reports only an aggregate number). Place it as a new Section 6 appended after the existing Section 5 single-tail block (after line 442), reusing — without redefining — the already-computed rate-normalized metric (`_METRIC`/`_UNIT`/`_USE_RATE`, kg/h when `time_sec_cruise` is present, else absolute kg) and the existing `_MIN_BASELINE_FLIGHTS`/`_AMBER_FACTOR`/`_RED_FACTOR` constants, the `_dnm` display_name helper, AC_COL, and `df` already in scope. Guard the whole block: if not (cruise_cols and AC_COL and 'date' in df.columns) render st.info and skip. Build `df_w = df.dropna(subset=['date', _METRIC, AC_COL]).copy()`; for each tail group g = sort_values('date'): compute n_base = min(len(g), max(_MIN_BASELINE_FLIGHTS, int(np.ceil(len(g)*0.30)))) and recent_n = min(_MIN_BASELINE_FLIGHTS, len(g)); ENFORCE TEMPORAL SEPARATION per the documented lesson — only include the tail when len(g) >= n_base + recent_n so the baseline (earliest n_base flights) and recent (last recent_n flights) windows do NOT overlap, otherwise skip it and count it as excluded-for-thin-coverage; baseline = float(g[_METRIC].iloc[:n_base].median()); skip if baseline <= 0; recent_mean = float(g[_METRIC].iloc[-recent_n:].mean()); pct_above = (recent_mean - baseline)/baseline*100; status = '🔴 >+10%' if recent_mean > baseline*_RED_FACTOR else '🟡 >+5%' if recent_mean > baseline*_AMBER_FACTOR else '🟢 OK'. Collect into a DataFrame, sort by pct_above descending, cap at top 12 (st.caption noting 'Top 12 of N analysed; M excluded for thin/overlapping history' when applicable). Render: a st.columns(2) count row ('🔴 Aircraft >10% above own baseline', '🟡 Aircraft 5–10% above') computed over ALL qualifying tails (not just the top 12), then a st.dataframe with columns Aircraft (display via _dnm, never raw serial), Status, Recent {_UNIT} ({:.0f}), Own baseline {_UNIT} ({:.0f}), % above ({:+.1f}%), Flights (len g), styled so red/amber rows are tinted. Empty state st.success('✅ No aircraft is currently burning above its own cruise baseline.') when none qualify. Honest English caption: this ranks tails by how far their recent cruise burn sits above their OWN historical baseline (rate-normalized by cruise time when available), the fleet-level companion to the single-aircraft Section 5 and a per-tail-baseline alternative to Section 2's fleet-relative ranking; it is an ADVISORY/monitor signal only — cruise fuel burn is still confounded by weight, altitude and wind, so it is NOT equivalent to a trained-model removal alert. All text English; numpy/pd/st already imported; purely additive; every existing section/KPI untouched; column-presence, min-flights, non-overlap, divide-by-zero, empty-df and NaN guards in place; <120 line diff.
- Files: `dashboard/pages/4_Fuel.py`
- Rationale: Diversifies away from the SAV/FOQA pages worked in the last 3 runs and directly applies two documented lessons: (1) 'um KPI de contagem agregada tem ação limitada se não expõe as entidades — sinais acionáveis devem listar os MSNs específicos' (the c4 count never names the tails), and (2) own-baseline degradation must be rate-normalized with temporal separation between baseline and recent windows. It reuses the page's existing rate-normalized metric and constants (single source of truth, no divergent heuristic), turns an opaque headline number into an actionable 'inspect these tails first' ranking, and is strictly more honest than Section 2's fleet-relative ranking — all while staying advisory per the fuel-confound lessons. Verified in full that no fleet-wide own-baseline ranking already exists on the page.
- Self-eval: alignment=6, value=6, quality=7, risk=2

## 2026-06-21 07:00
- **[Tier 5]** Rework Section 6 'Model Track Record' (_load_track_record + render) to attribute each TRAX removal to its OWN gear position instead of OR-ing all six prediction columns. Add a module-level _POSITION_TO_KEY map (faithfully replicating the producer's _POSITION_MAP variants -> canonical 'MLG 1'..'RH NLG' -> safe key mlg1/mlg2/mlg3/mlg4/nlg_lh/nlg_rh in _wheel_brake_data_prep.py, with a 'keep in sync with producer' comment) plus a _resolve_pos_key(raw) helper (strip+upper lookup). In _load_track_record: keep the report long-format aware (one prediction_<key> non-null per row) but DROP the rep['_any_pred']=max(...) OR; instead, per removal resolve its POSITION via _resolve_pos_key -> col='prediction_'+key, then within the 90-day pre-removal window select pos_rows = sub[sub[col].notna()] and set caught = (pos_rows[col]==1).any() with lead = (td - first flagged date).days. Add a new honest status '⚫ Position not mapped' (raw POSITION absent from the map) and keep '⚫ No telemetry in window' (no flights of that position in window); both are non-evaluable and excluded from the catch rate. Catch-rate KPIs (Caught/Catch rate/Median lead) now computed only over evaluable per-position removals. Update the Section 6 caption: replace 'Aircraft-level OR aggregation across all six gear positions' with 'Per-position attribution — each removal is matched only to its own gear-position model' and note that unmapped positions are excluded (not counted as catches or misses). All other sections untouched; no new imports (np/pd/load/display_name already present); all existing empty-df/missing-column/NaN guards intact.
- Files: `dashboard/pages/2_Wheels_and_Brakes.py`
- Rationale: The most emphatic recent lessons say to stop stacking heuristic lenses and instead make the validated, ground-truth KPIs rigorous. Section 6 is the page's commercial proof (predicted vs confirmed TRAX removals), but its headline catch rate uses an OR across all 6 positions — a documented systematic false-catch: a removal at MLG3 is scored 'caught' if ANY position (e.g. NLG) was flagged in the window. Memory explicitly prescribes per-position join for a track-record/catch KPI (reserving OR only for safety alerts), accepting a few '⚫ not mapped' rows as the honest cost. I read the producer to copy the exact TRAX position vocabulary rather than guess names, and the report's long layout (one prediction_<key> non-null per row) makes per-position attribution clean. This raises the trustworthiness of the client-facing catch rate without inventing data or adding another weak signal, and it deliberately avoids the 06-10 OR-vs-position failure pattern by applying the correct aggregation for this KPI type. The page was not touched in the last 3 runs.
- Self-eval: alignment=8, value=6, quality=7, risk=3

## 2026-06-21 07:00
- **[Tier 3]** Tornar honesto o KPI comercial headline (c6 'Unsched. removals flagged') e tratar o sinal de fuel cruise-burn consistentemente como tier advisório, conforme duas lições documentadas. (1) Linha 373: substituir `predictive_catches = len(sav_catch_msns | oxy_nodispatch_msns | wnb_catch_msns | fuel_catch_msns) + len(airbus_catch | sav_a320_catch)` por duas definições — `asserted_catches = len(sav_catch_msns | oxy_nodispatch_msns | wnb_catch_msns) + len(airbus_catch | sav_a320_catch)` e `fuel_advisory_n = len(fuel_catch_msns)` — com comentário de uma linha explicando que o fuel cruise-burn own-baseline é excluído do headline por ser heurístico sem ligação a falha confirmada (mantendo SAV/W&B/A320 SAV = predições de modelo, oxy amber = CAS no-dispatch e Airbus FOQA = exceedances de limite certificado como os catches assertados). (2) Linha 395-401: trocar `predictive_catches` por `asserted_catches` no `c6.metric(...)` e remover a cláusula 'or fuel cruise-burn >10% above own baseline' do help text, deixando-o descrever apenas predições de pré-falha de modelo, no-dispatch CAS e exceedances de limite certificado. (3) Caption de impacto comercial (linhas 403-407): acrescentar via f-string uma frase final separada — 'Separately, {fuel_advisory_n} aircraft are on a fuel cruise-burn advisory (>10% above their own baseline) — a monitor-tier heuristic confounded by weight/altitude/wind, not an asserted predictive catch.' (4) Banner Immediate Actions: remover o bloco de fuel level-2 das linhas 424-425 do loop `immediate_items`; no loop do Watch list (linhas ~442-451) tratar `fuel_alert.get(msn) in (1, 2)` num único ramo, renderizando '🔴'/'🟡' conforme o nível e mantendo o '+{fuel_pct}%' e o texto '(advisory — monitor engine degradation, ATA 73/76)', de modo que o fuel red-tier passe a aparecer em '⚠️ Monitor Closely' em vez de 'Immediate Actions Required'. Todo texto em inglês; sem novos imports; matriz de saúde (coluna Fuel) e demais seções intactas; `fuel_catch_msns`/`fuel_alert`/`fuel_pct` permanecem definidos e referenciados; ~30-40 linhas de diff.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: Aplica fielmente duas lições explícitas: (a) 'O KPI headline comercial não deve dobrar sinais heurísticos sem ligação a falha confirmada... manter o heurístico como tier advisório separado dos catches assertados' — hoje o c6 funde fuel own-baseline com predições de modelo validadas; (b) 'o burn de cruzeiro... classificar como monitor/advisory, nunca equiparar a um vermelho Immediate-Action de um modelo SAV treinado' — hoje o fuel red-tier está em st.error 'Immediate Actions Required'. É a integridade do número comercial #1 na página executiva, não tocada desde 2026-06-10, evitando a proliferação de mais um sinal fraco que as lições penalizam. Aditiva/corretiva: o sinal de fuel não é removido, é realocado ao seu tier honesto.
- Self-eval: alignment=7, value=6, quality=8, risk=2

## 2026-06-22 07:00
- **[Tier 3]** Make the c4 headline KPI 'Aircraft with rising cruise-burn trend (>5%)' derive from the EXACT same own-baseline calculation as the Section 6 Cruise-Burn Watchlist, removing the divergent np.polyfit (absolute-kg, min-3-flight) method. Lift the _USE_RATE/_METRIC/_UNIT/_Y_TITLE selection, the _MIN_BASELINE_FLIGHTS/_AMBER_FACTOR/_RED_FACTOR constants, and the cached _build_cruise_watchlist function to ABOVE the KPI row; build the watchlist once up top (guarded by cruise_cols/AC_COL/date present), set n_rising = count of tails whose Status is red+amber (recent cruise burn >5% above their own baseline), and render c4 from it with help text noting it shares Section 6's method so headline and named MSNs always agree. Section 5 keeps only its existing captions (which read the now-earlier _USE_RATE); Section 6 reuses the up-top wl_all/_wl_excluded instead of rebuilding. Empty/guard states preserved; all text English; no new imports (np/pd already present).
- Files: `dashboard/pages/4_Fuel.py`
- Rationale: Run 2026-06-20T04 shipped Section 6 with a self-evaluation flagging a real, still-open defect: c4 and the watchlist use different math, so the headline number and the drill-down MSN list disagree while the caption claims one explains the other — a trust-eroding contradiction a maintenance manager would catch in a demo. The documented would_do_differently was exactly 'make Section 6 and the c4 count derive from the same calculation'. This consolidates two overlapping cruise-burn signals into one (directly following the meta-lesson to stop stacking divergent lenses) rather than adding a new heuristic, and is the highest-integrity bounded change available within dashboard/ given the confirmed-failure-validation lever is blocked (no producer exports e2_sav_confirmed_failures to Drive).
- Self-eval: alignment=7, value=6, quality=6, risk=3

## 2026-06-22 07:00
- **[Tier 1]** Replace 1_SAV.py's hardcoded, currently-false freshness caption with an honest refresh-time badge driven by the Drive report's modifiedTime, and introduce the reusable helper as additive functions in dashboard/utils/drive_loader.py (existing load/clean_df/make_prefix_map UNTOUCHED, so zero blast radius on other pages). (1) In drive_loader.py append two ADDITIVE functions: `get_file_mtime(filename) -> pd.Timestamp|None` — `@st.cache_data(ttl=900, show_spinner=False)`, reuse `_get_service()` + `st.secrets['GOOGLE_DRIVE_FOLDER_ID']`, query `fields='files(id,name,modifiedTime)'` `orderBy='modifiedTime desc'`, return `pd.to_datetime(files[0]['modifiedTime'], utc=True)` or None (try/except → None, empty list → None); and `render_freshness_badge(filename, *, fresh_hours=6, stale_hours=48, label=None) -> float|None` — compute `age_h = (pd.Timestamp.now(tz='UTC') - mtime)/pd.Timedelta(hours=1)`; if mtime is None → `st.caption('Source refresh time unavailable.')` and return None; `st.success(f'✅ {label or filename} refreshed {age_h:.0f}h ago')` when age_h<=fresh_hours; `st.caption(f'{label or filename} refreshed {age_h:.0f}h ago')` when age_h<=stale_hours; else `st.warning(f'⚠️ {label or filename} not refreshed in {age_h:.0f}h — the producing job may be stopped; predictions below may be outdated')`; return age_h. All English. (2) In 1_SAV.py: add `get_file_mtime, render_freshness_badge` to the existing `from utils.drive_loader import ...` line; replace the whole `_latest_flight_date(...)` caption block (~lines 78-90), DELETING the static '· auto-refreshed hourly' claim and the date-inversion-prone 'Data through {max date}' line, with a single call that badges on the OLDER of the two SAV reports: `_lh_m, _rh_m = get_file_mtime('e2_sav_lh_report.parquet'), get_file_mtime('e2_sav_rh_report.parquet')`; pick the report whose mtime is older (skip None) and call `render_freshness_badge(<older file>, label='SAV report')` so the badge sits ABOVE the fleet-wide alert section (naturally propagating the stale state to the headline alerts). Keep stale_hours=48 to match the pipeline audit's threshold; this keys off refresh time (job runs hourly regardless of new flights), so a stale mtime distinguishes 'job stopped' from 'fleet idle'. (3) Per the documented lesson, grep the ENTIRE 1_SAV.py for any other static cadence/freshness claims ('hourly', 'auto-refreshed', 'updated every', hardcoded model-name freshness) and remove/honest-ify them in the same pass. No new dependencies; ~55-line diff total.
- Files: `dashboard/pages/1_SAV.py`, `dashboard/utils/drive_loader.py`
- Rationale: Tier 1 is at 0% and MUST advance before any higher tier (Rule #1); two sources feeding this page (e2_sav_lh/rh) are stale 50h, so Rule #2 mandates surfacing that NOW over any aesthetic work. 1_SAV.py currently lies — its caption asserts 'auto-refreshed hourly' while the source job is dead 50h — exactly the documented defect ('cadence claims must not be hardcoded; they lie when the pipeline fails'). Keying the badge off Drive modifiedTime (not the corrupted df['date'], which clean_df documents as inverted to Dec-2026) is both honest and trap-free, and turns a confidence tweak into an actionable 'job may be stopped / predictions outdated' warning. Putting the logic in a shared utils helper (not per-page copy-paste) follows the explicit lesson that cross-cutting freshness indicators must be a shared helper, and sets up consistent rollout to the four badge-less pages in later runs. 1_SAV.py is not among the last 3 runs and its only recent touch was reverted, satisfying diversification.
- Self-eval: alignment=8, value=7, quality=8, risk=2

## 2026-06-23 07:00
- **[Tier 1]** Rework the bottom 'Data freshness' expander (lines 728-756) into an honest Pipeline Health panel driven by each report's real Drive refresh time, not the max event date. (1) Add get_file_mtime to the existing `from utils.drive_loader import ...` line (load/make_prefix_map/display_name/clean_df). (2) Define a module-level list of (label, df_key, filename, producing_job) tuples covering all 10 sources, with the real producing Dagster job name (save_sav_report → e2_sav_lh/rh; save_oxy_report → oxy; e2_foqa_moqa_job → foqa; save_wheel_brake_report → wnb; save_fuel_consumption_report → fuel; airbus_foqa_moqa_job → a320/a330; save_airbus_sav_report → sav_a320_e1/e2). (3) Replace the loop: for each source call get_file_mtime(filename) to compute refresh age_h; classify Status as 'Stale' when age_h>48 (or mtime None → 'Unavailable'), 'Aging' when 24<age_h<=48, else 'Active'. Build a pandas DataFrame with columns System / Status / Producing job / Last refresh (e.g. '62h ago' or '—') / Last flight (existing df_chk['date'].max() formatted, kept as secondary data-coverage context per the lesson distinguishing refresh-time from last-event-date) / Rows, rendered via st.dataframe (text-only status, no emoji). Remove the broken empty-string `status` line. (4) Above the expander, compute n_stale = count of sources with age_h>48 or mtime None and render an actionable headline: st.warning(f'{n_stale} of {n_total} data sources have not refreshed in 48h+ — the producing job(s) may be stopped; predictions on the affected pages may be outdated.') when n_stale>0, else st.success('All data sources refreshed within 48h.'). Keep section under a `:material/health_and_safety: Pipeline health` subheader. All text English; no new dependencies; matrix/alerts/KPIs untouched.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: Tier 1 is the gating tier at 7% and Rule 2 mandates surfacing the stale SAV reports (62h) before any aesthetics. The existing panel measures the wrong signal (last event date, not refresh time), has a dead empty-string status that renders nothing, and is the documented Tier-1 open item 'which jobs are alive'. Reworking it with the already-built get_file_mtime reuses last run's helper (closing that lesson), fixes a real rendering bug, names the dead producing job so the operator knows what to restart, and makes the stale SAV pipeline impossible to miss on the executive landing page — actionable insight, not just a per-page badge.
- Self-eval: alignment=8, value=7, quality=5, risk=4

## 2026-06-23 07:00
- **[Tier 1]** Bring data-freshness-badge coverage to 100% of pages by extracting the 'pick the oldest report' glue into a single shared helper (the documented debt) and wiring it into the 4 pages that still lack a badge. (1) In dashboard/utils/drive_loader.py append ONE additive function (existing load/clean_df/make_prefix_map/display_name/get_file_mtime/render_freshness_badge UNTOUCHED): `render_freshest_badge(filenames, *, label=None, fresh_hours=6, stale_hours=48) -> float|None` — accept a list of report filenames (a single-source page passes a 1-element list); call the existing cached `get_file_mtime(f)` for each, keep only those whose mtime is not None, and if at least one exists delegate to the existing `render_freshness_badge(<file with the OLDEST mtime>, fresh_hours=fresh_hours, stale_hours=stale_hours, label=label)` so the badge always reflects the most-stale source feeding the page; if every mtime is None, call `render_freshness_badge(filenames[0], label=label)` (which already emits 'Source refresh time unavailable.'). No new imports (pd/st/get_file_mtime/render_freshness_badge already in module). First confirm the helper signatures actually match before delegating. (2) Wire the badge near the top of each page (immediately below the page st.title/header, ABOVE the first data section so a stale state naturally precedes any predictions), adding `render_freshest_badge` (and `get_file_mtime` only if needed) to each page's existing `from utils.drive_loader import ...` line: 3_Oxygen.py -> `render_freshest_badge(['e2_oxy_report.parquet'], label='Oxygen report')`; 4_Fuel.py -> `render_freshest_badge(['e2_fuel_report.parquet'], label='Fuel report')`; 7_SAV_A320.py -> `render_freshest_badge(['airbus_sav_eng1_report.parquet','airbus_sav_eng2_report.parquet'], label='A320 SAV report')`; 8_Airbus_FOQA.py -> `render_freshest_badge(['airbus_a320_foqa_report.parquet','airbus_a330_foqa_report.parquet'], label='Airbus FOQA report')`. All text English; keep stale_hours=48 to match the pipeline audit threshold (the badge keys off refresh time, so a stale mtime distinguishes 'job stopped' from 'fleet idle'); no other lines on any page touched; no new dependencies. Total diff ~30-45 lines across 5 files.
- Files: `dashboard/pages/3_Oxygen.py`, `dashboard/pages/4_Fuel.py`, `dashboard/pages/7_SAV_A320.py`, `dashboard/pages/8_Airbus_FOQA.py`, `dashboard/utils/drive_loader.py`
- Rationale: Tier 1 is at 14% (far below the 80% gate to advance), and 'badge de frescor por página' is an explicit open Tier-1 item. Exactly 4 pages (3_Oxygen, 4_Fuel, 7_SAV_A320, 8_Airbus_FOQA) still lack a badge per the dashboard state — closing them reaches 100% coverage, a concrete completable milestone. This is forward progress on un-recently-touched pages (diversifies away from 0_Fleet_Overview/1_SAV/4_Fuel-heavy recent runs) rather than re-fixing my own last-run code. It also pays down a documented lesson directly: the 'choose the oldest report' logic that became ~15 lines of glue on 1_SAV belongs in the shared util as render_freshest_badge — so this both ships the feature AND removes duplication, reusing the existing cached get_file_mtime (no extra Drive I/O, no new deps). 7_SAV_A320 in particular gains genuine safety value: if the airbus_sav job dies, A320 SAV predictions would otherwise go stale silently.
- Self-eval: alignment=7, value=7, quality=7, risk=3

## 2026-06-29 07:00
- **[Tier 1]** Repoint the executive Fleet Overview's E2 SAV signal from the 218h-dead, junk-inflated aggregate reports to the fresh (3.5h) ACARS-validated transient reports the SAV page already trusts — with the empirical verification and coverage guard the 06-27 run lacked. (1) PIPELINE_SOURCES lines 26-27: replace the two aggregate entries with ('SAV LH (E2)','sav_lh','e2_sav_transient_lh_report.parquet','save_sav_transient_report') and ('SAV RH (E2)','sav_rh','e2_sav_transient_rh_report.parquet','save_sav_transient_report') so the pipeline-health panel monitors the source actually consumed (clears the permanent false-stale on parquets nothing renders). (2) _load_all dict lines 43-44: point keys sav_lh/sav_rh to the same two transient filenames. (3) Add module-level constants _SAV_HIGH = 0.60 (mirrors 1_SAV.py _THRESHOLD_HIGH, the documented calibrated High band) and _SAV_RECENCY_DAYS = 30. (4) SAV alert build lines 272-288: VERIFIED by reading both parquets — transient keys on ac_sn (clean 5-digit suffix, same format as aggregate after the existing .0 strip; aggregate's 77 was junk-inflated with '0'/'12'/'13'/'14'), carries sav_transient_prob + last_flight_dt, and has NO sav_transient_alert/alert_threshold column, so mirror 1_SAV.py _is_alert's fallback: alert = sav_transient_prob >= _SAV_HIGH. Take the latest row per ac_sn (transient is already 1 row/ac), normalize ac_sn with str.strip()+strip trailing .0, and apply a RECENCY GUARD using last_flight_dt (pd.to_datetime, errors=coerce): mark alert=1 only when last_flight_dt >= date.today() - timedelta(days=_SAV_RECENCY_DAYS), so a stale flight's prediction cannot become a permanent false alert. Build sav_lh_alert/sav_rh_alert as {ac_sn:1|0} keeping the EXACT dict shape so ALL downstream consumers (sav_catch_msns, n_sav_alert KPI, immediate_items, matrix cells) stay untouched; guard for empty df / missing columns as the current code does. (5) COVERAGE ASSERTION (the missing piece that got 06-27 reverted): compute n_sav_cov = size of the union of ac_sn scored across lh+rh, and render a visible st.caption near the SAV signal/health panel — ':material/dataset: SAV (E2): {n} aircraft scored by the ACARS-validated transient model.' — and when n_sav_cov == 0 emit an st.warning so a coverage collapse fails VISIBLE, never as silent zero-alerts. English text, Material Symbols only, date/timedelta already imported (no new imports/deps), ~45-60 line diff confined to this file.
- Files: `dashboard/pages/0_Fleet_Overview.py`
- Rationale: This is the single highest-value Tier 1 defect and the ONLY stale source in the freshness scan (e2_sav_lh/rh_report at 218h), so rule 2 mandates fixing it before any aesthetic. The flagship executive landing page currently presents 9-day-dead, junk-serial-inflated SAV predictions as if live. The prior 06-27 attempt was reverted (user_value 6) ONLY for unverified ac_sn-format/coverage/schema assumptions — I have now empirically verified all three (format matches; transient = clean 40 E2 aircraft 1-row/ac vs the aggregate's 77 junk-padded; schema = sav_transient_prob with no alert col, threshold 0.60) and the plan bakes in the coverage assertion + recency guard that the run's own would_do_differently demanded. Keeping the {ac_sn:1|0} dict shape leaves every downstream consumer untouched, holding risk low.
- Self-eval: alignment=8, value=7, quality=7, risk=4

## 2026-07-02 07:00
- **[Tier 1]** Replace the home-rolled event-date freshness block (lines 75-89, which derives 'may be stale' from df['date'].max() + a fixed 2-day threshold) with the shared mtime-based render_freshest_badge, and surface the currently-invisible dead TRAX maintenance feed. (1) Add render_freshest_badge to the import on line 17 (from utils.drive_loader import load, make_prefix_map, display_name, clean_df, render_freshest_badge). (2) In the block at lines 75-89: emit render_freshest_badge(['e2_wnb_report.parquet'], label='Wheels & Brakes report') as the pipeline-refresh verdict (keys off Drive modifiedTime, so it detects a stopped save_wheel_brake_report and never cries wolf on idle weekends — consistent with the 8 other pages); KEEP a neutral st.caption(f'Latest flight event: {_latest_event:%d-%b-%Y}') as data context ONLY (drop the st.success/st.warning 'may be stale' verdict derived from event date, per the documented 'distinguish max-date from last-refresh' lesson); and add a SECOND, distinctly-labeled render_freshest_badge(['e2_wnb_maintenance.parquet', 'e2_brake_maintenance.parquet'], label='TRAX removal/maintenance data') so the 90h-dead maintenance/removal ground-truth feed (loaded at lines 517-518 and 721-724, currently with no freshness signal) fails visible as an honest 'not refreshed in Nh — producing job may be stopped' warning. Guard the _latest_event caption for NaT as the current code does. English text, Material Symbols/text only (no emoji), no new deps; ~20-30 line diff confined to this file.
- Files: `dashboard/pages/2_Wheels_and_Brakes.py`
- Rationale: State snapshot was stale: all 9 pages already carry render_freshest_badge and the shared helper already exists — re-adding badges would be redundant. Empirically, 2_Wheels_and_Brakes is the ONE page still using a non-standard event-date freshness indicator that (a) cannot detect a stopped job, (b) cries wolf on weekends via a magic 2-day threshold, and (c) is inconsistent with the rest of the product. Its TRAX maintenance feeds are genuinely 90h dead right now yet have zero pipeline-freshness signal, violating Principle 1 and Rule 2 (make stale sources visible). Fixing the indicator to key off refresh-time AND surfacing the dead maintenance feed is actionable integrity, not a cosmetic badge-add. Page not touched in the last 3 runs (diversification), and it's the concrete remaining Tier-1 gap that moves 28% toward 80%.
- Self-eval: alignment=8, value=7, quality=6, risk=2

## 2026-07-02 07:00
- **[Tier 1]** Fix the flagship AI Fleet Briefing page so it actually renders in the deployed product and stops stating false/stale facts. (1) Replace the hardcoded BRIEFINGS_DIR = r'd:\Predictive_analytics\dashboard\briefings' (line 11) with a path resolved relative to the page file: BRIEFINGS_DIR = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), 'briefings') — on Streamlit Cloud the repo root is the dashboard/ contents (verified: briefings/current.md + dated archives are git-tracked in the deploy repo), so the absolute Windows path never exists there and pins the page to a permanent 'No briefing available yet' empty state. This also stays correct locally (resolves to the same dashboard/briefings). (2) Correct the false model-name fact 'Claude Sonnet 4.6' -> 'claude-opus-4-8' in both the module docstring (line 3) and the caption (line 15), per the canonical CLAUDE.md model id. (3) Make 'Last updated' / staleness honest in deploy: add `import re`; parse the most-recent dated archive filename (pattern r'(\d{4}-\d{2}-\d{2})_(\d{2})(\d{2})\.md', e.g. 2026-05-28_1801.md -> 2026-05-28 18:01) into a datetime used as the generation time for BOTH the 'Last updated' caption and the age/staleness banner; fall back to os.path.getmtime(current.md) ONLY when no dated archive exists, because in deploy the current.md mtime is the git checkout time, not the generation time. Leave the empty-state st.stop guard, the st.markdown(briefing) render, and the archive selectbox block untouched. All text English, Material Symbols only, no new dependencies; ~20-25 line diff confined to this file.
- Files: `dashboard/pages/6_Fleet_Briefing.py`
- Rationale: Rule 1 keeps us in Tier 1 (35%). The 7_SAV_A320 badge in the audit is already present (verified in-file), so no page consuming the 98.9h-stale airbus_sav reports is unguarded — Rule 2 is already satisfied. The remaining Tier-1 badge gaps (3_Oxygen/4_Fuel/8_Airbus_FOQA) all show FRESH data, where the memory lessons flag a badge as low-value confidence, not actionable insight. By contrast, the flagship executive briefing page is fully BROKEN in the deployed product (hardcoded Windows path -> permanent empty state) and displays a false model-name fact and a misleading 'Last updated' — a Tier-1 data-integrity + UI-veracity failure on the marquee sales-demo surface. Fixing it delivers real integrity value, not cosmetics, and completes a planned-but-never-applied (noop) corrective.
- Self-eval: alignment=8, value=7, quality=8, risk=2
