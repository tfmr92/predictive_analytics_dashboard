# Fleet Briefing — 2026-05-28

## 🚨 Immediate Actions Required
Nenhuma — dados FOQA disponíveis não mostram exceedances confirmadas com valores acima dos limites AMM nos 402 registros analisados. Verificar pipelines SAV/WNB/OXY/FUEL (arquivos parquet ausentes).

## ⚠️ Monitor Closely (próximos 5 voos)
- **FOQA — Todos os aviões:** estatísticas agregadas de ITT, N2-vib e aceleração de pouso indisponíveis por truncamento do summary. Reprocessar `e2_foqa_report.parquet` com `.describe()` completo para confirmar ausência de exceedances individuais nos campos `n2_vib_lh_amber`, `n2_vib_rh_amber`, `itt_lh_takeoff_exceedance`, `hard_landing_flag`.
- **Pipeline de dados:** 5 sistemas sem parquet (`e2_sav_lh`, `e2_sav_rh`, `e2_wnb`, `e2_oxy`, `e2_fuel`). Predições preditivas completamente cegas para SAV, W&B, oxigênio e combustível.

## ✅ Fleet Health Overview
FOQA/MOQA operacional com 402 voos registrados — cobertura de monitoramento ATA 05 ativa. Os demais sistemas (SAV LH/RH, Wheels & Brakes, Oxygen, Fuel) estão sem dados de predição por falha na geração dos parquets; não há como afirmar saúde ou risco desses componentes. Prioridade imediata: restaurar os 5 pipelines de relatório antes da próxima janela de decisão de manutenção.

## 📈 Trend Highlights
- **SAV:** Indisponível — `e2_sav_lh_report.parquet` e `e2_sav_rh_report.parquet` não encontrados em `D:\parquet_files\`.
- **W&B:** Indisponível — `e2_wnb_report.parquet` ausente.
- **Engine (FOQA):** 402 voos monitorados. Colunas de exceedance presentes (`itt_lh/rh_takeoff_exceedance`, `n2_vib_lh/rh_amber`, `n2_vib_lh/rh_borescope`, `hard_landing_flag`). Valores estatísticos não recuperados — requer inspeção direta do parquet.
- **Oxygen:** Indisponível — `e2_oxy_report.parquet` ausente.

## 🔧 Recommended Maintenance Actions

| Aircraft | System | Priority | Action |
|---|---|---|---|
| Toda a frota | Pipelines de dados | P1 | Verificar jobs Dagster: `save_sav_report`, `save_oxy_report`, `save_fuel_consumption_report`, `e2_wheel_brake_job` — nenhum gerou parquet em `D:\parquet_files\` |
| Toda a frota | FOQA | P2 | Executar `.describe()` no `e2_foqa_report.parquet` para extrair min/max de `max_n2_vib_lh/rh`, `max_itt_lh/rh` e `max_normal_accel_landing` por aeronave |
| A verificar | SAV LH/RH | P2 | Sem dados preditivos — verificar manualmente histórico de falhas de starter valve nas aeronaves com mais ciclos |

---
> **Nota:** Briefing com dados parciais. 5 de 6 sistemas sem parquet — provável falha nos schedules Dagster ou `D:\parquet_files\` não mapeado. Verificar `dagster-webserver` em `localhost:3000`.