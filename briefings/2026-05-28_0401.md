# Fleet Briefing — 2026-05-28

---

## 🚨 Immediate Actions Required

> **Dados insuficientes para ações imediatas confirmadas.** O resumo estatístico do FOQA foi truncado (apenas linha `count` disponível) — os valores de `max_itt`, `max_n2_vib` e `hard_landing_flag` não foram processados numericamente nesta execução. Reprocessar o parquet antes de tomar decisões operacionais.

---

## ⚠️ Monitor Closely (next 5 flights)

- **Parâmetros a observar conforme próximo run completo:** ITT takeoff LH/RH (limite: 1054°C), N2 vib LH/RH (amber: 4.0 AU), pressão de óleo (colunas `oil_press_lh_low` / `oil_press_rh_low` presentes nos dados).
- **Nota:** 402 voos FOQA registrados, porém 14 sem leitura de Mach (`max_mach` count=388 vs. 402) — investigar possível falha de sensor ADIRU em aeronaves afetadas.

---

## ✅ Fleet Health Overview

Pipeline FOQA operacional com **402 voos processados** no período. Os demais sistemas (SAV LH/RH, W&B, Oxigênio, Combustível) estão com parquets ausentes em `D:\parquet_files\` — os jobs de report correspondentes provavelmente não executaram ou falharam silenciosamente. Nenhuma excedência pode ser confirmada ou descartada sem os dados estatísticos completos.

---

## 📈 Trend Highlights

| Sistema | Status | Observação |
|---|---|---|
| **SAV LH/RH** | ❌ Indisponível | Parquet ausente — verificar `save_sav_report` no Dagster |
| **W&B** | ❌ Indisponível | Parquet ausente — verificar `e2_wheel_brake_job` |
| **Engine (FOQA)** | ⚠️ Parcial | 402 registros carregados, estatísticas truncadas |
| **Oxigênio** | ❌ Indisponível | Parquet ausente — verificar `save_oxy_report` |
| **Combustível** | ❌ Indisponível | Parquet ausente — verificar `save_fuel_consumption_report` |

---

## 🔧 Recommended Maintenance Actions

| Aircraft | Sistema | Prioridade | Ação |
|---|---|---|---|
| Frota | SAV / WNB / OXY / FUEL | **Alta** | Verificar no Dagster UI se os 5 jobs de report estão em estado `FAILURE` ou `SKIPPED`; confirmar que parquets existem em `D:\parquet_files\` |
| Frota | FOQA | **Média** | Re-executar briefing após obter estatísticas completas (mean/max/min) do parquet FOQA — hoje apenas `count` foi processado |
| 14 aeronaves | ADIRU / Mach | **Baixa** | Investigar ausência de `max_mach` em 14 voos (3,5% da frota) |

---

> **Próximo briefing:** aguardar restauração dos parquets ausentes. Verificar logs do Dagster em `D:\DAGSTER_HOME` para identificar causa raiz das falhas de pipeline.