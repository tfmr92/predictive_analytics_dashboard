# Fleet Briefing — 2026-05-27

## 🚨 Immediate Actions Required

Nenhuma ação imediata identificada com base nos dados disponíveis. Validar disponibilidade dos arquivos parquet para SAV, W&B, OXY e FUEL antes de confirmar status (ver seção de tendências).

---

## ⚠️ Monitor Closely (próximos 5 voos)

- **FOQA — Vibração N2 (LH/RH):** Colunas `n2_vib_lh_amber` / `n2_vib_rh_amber` presentes nos 20 registros — verificar aeronaves com flags ativos; limite âmbar = 4.0 AU.
- **FOQA — ITT Decolagem:** Campos `itt_lh_takeoff_exceedance` / `itt_rh_takeoff_exceedance` registrados — confirmar se algum evento ultrapassou 1054 °C.
- **FOQA — Landing G:** `max_normal_accel_landing` e `hard_landing_flag` disponíveis — revisar qualquer evento próximo de 2.0 g com peso de pouso acima de 48.000 kg.

---

## ✅ Fleet Health Overview

Os dados FOQA dos últimos 30 dias cobrem **20 voos** com telemetria completa de fases de voo, ITT, vibração N2 e aceleração de pouso. Os demais sistemas de monitoramento preditivo (SAV, W&B, OXY, FUEL) estão com parquet ausente em `D:\parquet_files\` — os pipelines correspondentes precisam ser executados antes da próxima janela de briefing. Sem os dados de predição ML, não é possível avaliar tendências de pré-falha para válvulas de partida, freios e oxigênio.

---

## 📈 Trend Highlights

- **SAV:** Dados indisponíveis — `e2_sav_lh_report.parquet` e `e2_sav_rh_report.parquet` ausentes. Verificar schedules `save_sav_report` no Dagster.
- **W&B:** Dados indisponíveis — `e2_wnb_report.parquet` ausente. Verificar schedule `e2_wheel_brake_job`.
- **Engine (FOQA):** 20 registros ativos. Colunas de exceedance (`mmo_exceedance`, `vmo_exceedance`, `vle_exceedance`, `n2_vib_*`) presentes — agregação completa requer leitura direta do parquet para confirmar contagens positivas.
- **Oxygen:** Dados indisponíveis — `e2_oxy_report.parquet` ausente. Verificar schedule `save_oxy_report`.

---

## 🔧 Recommended Maintenance Actions

| Aeronave | Sistema | Prioridade | Ação |
|---|---|---|---|
| Frota geral | Pipeline de dados | Alta | Executar manualmente os jobs `save_sav_report`, `save_oxy_report`, `save_fuel_consumption_report` e `e2_wheel_brake_job` no Dagster UI para regenerar parquets ausentes |
| A verificar (FOQA) | Motor LH/RH | Média | Consultar `e2_foqa_moqa_computed_parameters` diretamente para filtrar `n2_vib_lh_amber = True` ou `hard_landing_flag = True` e acionar inspeção AMM ATA 05 nos casos positivos |
| A verificar (FOQA) | ITT | Média | Verificar registros com `itt_lh_takeoff_exceedance = True` — se ITT > 1054 °C confirmado, acionar procedimento AMM ATA 72 |

---

> **Nota operacional:** Este briefing está parcialmente limitado pela ausência de 4 dos 5 arquivos parquet de predição. Recomenda-se verificar o status dos daemons Dagster (`dagster-daemon run`) e reprocessar os sensors/schedules pendentes antes do próximo ciclo de briefing (13:00).